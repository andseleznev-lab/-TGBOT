# План реализации: Улучшение UI текстов и стилей интерфейса

**Тикет**: T-004
**PRD**: `docs/prd/T-004.prd.md`
**Дата создания**: 2026-02-11
**Статус**: COMPLETED

## Краткое описание

Обновление текстов интерфейса и CSS стилей для улучшения пользовательского опыта: изменение заголовков, описаний услуг, удаление emoji иконок, обновление стилей календаря.

## Архитектура решения

### Обзор

Изменения затрагивают только frontend:
1. Обновление строковых констант в коде (заголовки, описания)
2. Удаление emoji иконок из конфигурации
3. Обновление CSS стилей для календаря (форма, цвет, анимации)

Никаких изменений в логике, API запросах или backend не требуется.

## Затрагиваемые файлы

### Изменяемые файлы

**Frontend**:

- `app.js`
  - **Этап 1**: Изменить заголовок "Выберите услугу" на "Диагностика - это первый шаг перед консультацией" (строка 1296)
  - **Этапы 3-4**: Обновить функцию `getServiceDescription()` (строки 2670-2679)
    - "Пакет консультаций": изменить описание
    - "Индивидуальная консультация": изменить описание

- `config.js`
  - **Этап 2**: Удалить все emoji из объекта `SERVICE_ICONS` (строки 75-80)
  - Оставить пустой объект или убрать использование иконок в renderServicesScreen

- `styles.css`
  - **Этап 5**: Обновить стили календаря (строки 424-481)
    - `.calendar-day.available` - изменить на круглую форму, салатовый цвет
    - `.calendar-day.selected` - добавить эффект увеличения (transform: scale())
    - `.slots-indicator` - скрыть или удалить (display: none)
    - Добавить CSS animation для эффекта выбора даты

### Новые файлы

_Не требуются._

---

## Изменения UI

### Этап 1: Заголовок экрана услуг

**Текущий HTML** (app.js:1296):
```javascript
<h1 class="screen-title fade-in">Выберите услугу</h1>
```

**Новый HTML**:
```javascript
<h1 class="screen-title fade-in">Диагностика - это первый шаг перед консультацией</h1>
```

**CSS**: Без изменений (используем существующий класс `.screen-title`)

---

### Этап 2: Удаление emoji иконок

**Текущий config.js** (строки 75-80):
```javascript
SERVICE_ICONS: {
    'Диагностика': '🎯',
    'Пакет консультаций': '📦',
    'Семейная консультация': '👨‍👩‍👧',
    'Индивидуальная консультация': '💼'
}
```

**Новый config.js**:
```javascript
SERVICE_ICONS: {
    // Иконки убраны для чистого дизайна
}
```

**Изменение в app.js** (строка 1301):
```javascript
// Было:
<div class="service-icon">${CONFIG.SERVICE_ICONS[service.name] || '📋'}</div>

// Станет (скрываем блок если нет иконки):
${CONFIG.SERVICE_ICONS[service.name] ? `<div class="service-icon">${CONFIG.SERVICE_ICONS[service.name]}</div>` : ''}
```

---

### Этап 3: Описание "Пакет консультаций"

**Текущий код** (app.js:2674):
```javascript
'Пакет консультаций': '10 индивидуальных сессий со скидкой 25%',
```

**Новый код**:
```javascript
'Пакет консультаций': 'Лучшее решение для работы с психологом',
```

---

### Этап 4: Описание "Индивидуальная консультация"

**Текущий код** (app.js:2676):
```javascript
'Индивидуальная консультация': 'Персональная встреча один на один, 1 час'
```

**Новый код**:
```javascript
'Индивидуальная консультация': 'Персональная консультация с психологом, 1 час'
```

---

### Этап 5: Стили календаря

#### Изменения в styles.css

**5.1. Доступные даты (`.calendar-day.available`)** - строки 424-431

Текущий стиль:
```css
.calendar-day.available {
    background: linear-gradient(135deg, #A8C686, #C5E1A5) !important;
    border: 3px solid #7FA860 !important;
    border-radius: 12px; /* Квадратная форма */
    /* ... */
}
```

Новый стиль:
```css
.calendar-day.available {
    background: linear-gradient(135deg, #C5E1A5, #A8D08D) !important; /* Салатовый */
    border: 2px solid #8BC34A !important;
    border-radius: 50% !important; /* Круглая форма */
    color: var(--text-primary) !important;
    font-weight: 700 !important;
    box-shadow: 0 4px 12px rgba(139, 195, 74, 0.5) !important;
    animation: pulse-available 2s ease-in-out infinite !important;
}
```

**5.2. Выбранная дата (`.calendar-day.selected`)** - строки 450-455

Добавить эффект увеличения:
```css
.calendar-day.selected {
    background: var(--accent-gradient);
    color: var(--text-white);
    border-color: var(--accent-primary);
    border-radius: 50% !important; /* Круглая форма */
    box-shadow: 0 4px 12px rgba(212, 165, 116, 0.4);
    transform: scale(1.15); /* Эффект увеличения */
    animation: scale-in 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55); /* Анимация появления */
}
```

**5.3. Анимация выбора даты** (новая)

Добавить после `.calendar-day.selected`:
```css
@keyframes scale-in {
    0% {
        transform: scale(0.8);
        opacity: 0.5;
    }
    50% {
        transform: scale(1.25); /* Чуть больше увеличиваем */
    }
    100% {
        transform: scale(1.15);
        opacity: 1;
    }
}
```

**5.4. Скрыть индикаторы слотов (`.slots-indicator`)** - строки 467-485

```css
.slots-indicator {
    display: none; /* Скрываем красные точки */
}

/* Убираем стили для .calendar-day.available .slots-indicator и .calendar-day.selected .slots-indicator */
```

---

## ⚡ Производительность

### Оптимизации

**Рендеринг**:
- Изменения не влияют на производительность
- Все изменения - статичные строки и CSS

**Памяти**:
- Минимальное влияние (удаляем emoji объект)

### Целевые метрики

- Время загрузки: без изменений
- Время отклика UI: без изменений
- Никакой дополнительной нагрузки

---

## Тестирование

### Что нужно протестировать

**Функциональное тестирование**:

- [ ] **Этап 1**: Заголовок "Диагностика - это первый шаг перед консультацией" отображается на экране услуг
- [ ] **Этап 2**: Карточки услуг отображаются без emoji иконок
- [ ] **Этап 3**: Описание "Пакет консультаций" изменено на "Лучшее решение для работы с психологом"
- [ ] **Этап 4**: Описание "Индивидуальная консультация" изменено на "Персональная консультация с психологом, 1 час"
- [ ] **Этап 5**:
  - Доступные даты в календаре имеют круглую форму и салатовый цвет
  - При выборе даты срабатывает эффект увеличения
  - Красные точки-индикаторы не отображаются

**Нефункциональное тестирование**:

- [ ] Все тексты читаемы на разных экранах (iPhone SE, iPhone 15 Pro Max)
- [ ] Заголовок не обрезается на маленьких экранах
- [ ] Анимации плавные (60 FPS)
- [ ] HapticFeedback работает при выборе даты

### Тест-кейсы

**TC-1: Проверка заголовка экрана услуг**
1. Открыть приложение
2. Перейти на вкладку "Услуги"
3. **Ожидаемый результат**: Заголовок "Диагностика - это первый шаг перед консультацией" отображается корректно

**TC-2: Проверка карточек услуг без emoji**
1. Открыть приложение
2. Перейти на вкладку "Услуги"
3. **Ожидаемый результат**:
   - Карточки отображаются без emoji иконок
   - Текст услуг выровнен корректно
   - Визуально карточки выглядят чисто

**TC-3: Проверка описаний услуг**
1. Открыть приложение
2. Перейти на вкладку "Услуги"
3. Найти карточку "Пакет консультаций"
4. **Ожидаемый результат**: Описание "Лучшее решение для работы с психологом"
5. Найти карточку "Индивидуальная консультация"
6. **Ожидаемый результат**: Описание "Персональная консультация с психологом, 1 час"

**TC-4: Проверка стилей календаря**
1. Открыть приложение
2. Перейти на вкладку "Запись"
3. Выбрать услугу
4. **Ожидаемый результат**:
   - Доступные даты имеют круглую форму
   - Цвет салатовый (#C5E1A5)
   - Нет красных точек-индикаторов
5. Нажать на доступную дату
6. **Ожидаемый результат**:
   - Дата увеличивается (scale: 1.15)
   - Плавная анимация появления
   - HapticFeedback `selectionChanged()` срабатывает

---

## ⚠️ Риски и митигация

### Технические риски

**Риск 1: Длинный заголовок может не поместиться на маленьких экранах**
- **Вероятность**: Средняя
- **Влияние**: Низкое
- **Митигация**: Проверить на iPhone SE (самый маленький экран), при необходимости уменьшить font-size для `.screen-title`
- **План Б**: Сократить текст до "Диагностика - первый шаг перед консультацией"

**Риск 2: Круглые даты могут выглядеть слишком мелкими**
- **Вероятность**: Низкая
- **Влияние**: Среднее
- **Митигация**: Тестирование на реальном устройстве, при необходимости увеличить размер `.calendar-day`
- **План Б**: Вернуться к квадратной форме с border-radius: 20px (более скругленная)

---

## Решения и компромиссы

### Архитектурные решения

**Решение 1: Скрытие блока иконки вместо удаления**
- **Проблема**: Нужно убрать emoji, но структура HTML может сломаться
- **Рассмотренные варианты**:
  - Вариант А: Удалить весь блок `.service-icon` из HTML
    - Плюсы: Чистый код
    - Минусы: Может нарушить layout (flex/grid)
  - Вариант Б: Условно скрывать блок если нет иконки
    - Плюсы: Безопасно, сохраняет структуру
    - Минусы: Лишний HTML в коде
- **Выбранное решение**: Вариант Б (условное скрытие)
- **Обоснование**: Минимальные изменения, без риска сломать layout

**Решение 2: Сохранить animation pulse для доступных дат**
- **Проблема**: Нужно сделать календарь современным, но не слишком перегруженным анимациями
- **Выбранное решение**: Оставить pulse-available анимацию (уже есть в коде)
- **Обоснование**: Анимация помогает пользователю видеть доступные даты, не раздражает

---

## Документация

### Что нужно обновить после реализации

- [ ] Обновить комментарии в `config.js` объясняющие почему SERVICE_ICONS пустой
- [ ] Обновить комментарии в `styles.css` для календарных стилей

---

## Чек-лист готовности плана

Перед переходом к созданию tasklist убедись:

- [x] Описана архитектура решения
- [x] Расписаны все 5 этапов изменений
- [x] Указаны конкретные строки кода для изменений
- [x] Описаны CSS изменения с примерами кода
- [x] Учтены риски и описана митигация
- [x] Все решения задокументированы
- [x] Статус изменён на `PLAN_APPROVED`

**План готов! Можно переходить к созданию tasklist.**
