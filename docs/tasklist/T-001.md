# Tasklist: Улучшение обработки сетевых ошибок

**Тикет**: T-001
**PRD**: `docs/prd/T-001.prd.md`
**План**: `docs/plan/T-001.md`
**Дата создания**: 2026-01-31
**Статус**: TASKLIST_READY

---

## 📊 Прогресс

**Всего задач**: 31
**Выполнено**: 24
**Прогресс**: [24/31] (77%)

---

## ✅ Список задач

### Подготовка

- [x] 1. Добавить CONFIG.API_TIMEOUT в config.js
  - **Acceptance**:
    - В config.js добавлена константа `API_TIMEOUT: 10000`
    - Константа доступна через `CONFIG.API_TIMEOUT`
  - **Files**: `config.js:7`
  - **Estimate**: 5 мин

---

### Core: Функции обработки ошибок

- [x] 2. Создать функцию getErrorType(error, response)
  - **Acceptance**:
    - Функция определяет тип ошибки: ABORT, TIMEOUT, NETWORK, SERVER, CLIENT, UNKNOWN
    - Возвращает объект `{ type: string, message: string }`
    - Есть JSDoc комментарий
    - Обрабатывает все типы из плана (AbortError, TimeoutError, Load failed, HTTP статусы)
  - **Files**: `app.js:206`
  - **Estimate**: 20 мин

- [x] 3. Создать функцию showErrorPopup(message, retryFn)
  - **Acceptance**:
    - Показывает Telegram popup с сообщением
    - Добавляет кнопку "Повторить" если retryFn передан
    - Всегда добавляет кнопку "Отмена"
    - Вызывает retryFn при клике на "Повторить"
    - Есть JSDoc комментарий
  - **Files**: `app.js:244`
  - **Estimate**: 15 мин

- [x] 4. Создать вспомогательные функции showRetryIndicator/hideRetryIndicator
  - **Acceptance**:
    - showRetryIndicator создаёт overlay с индикатором загрузки через безопасные DOM API
    - Использует textContent (не innerHTML) для безопасности
    - hideRetryIndicator удаляет overlay
    - Есть JSDoc комментарии
  - **Files**: `app.js:273,298`
  - **Estimate**: 20 мин

- [x] 5. Добавить CSS стили для retry indicator
  - **Acceptance**:
    - Добавлены классы: .retry-overlay, .retry-content, .loader
    - Стили соответствуют beige палитре
    - Glassmorphism эффект применён для .retry-content
    - Анимация spinner работает
  - **Files**: `styles.css:647-680`
  - **Estimate**: 15 мин

- [x] 6. Создать функцию retryRequest(requestFn, delay)
  - **Acceptance**:
    - Показывает retry indicator перед повтором
    - Ждёт заданную задержку (default 2000ms)
    - Выполняет requestFn
    - Скрывает retry indicator после выполнения
    - Обрабатывает ошибки retry
    - Есть JSDoc комментарий
  - **Files**: `app.js:311`
  - **Estimate**: 20 мин

- [x] 7. Создать функцию handleNetworkError(error, context, retryFn, config)
  - **Acceptance**:
    - Определяет тип ошибки через getErrorType()
    - Логирует детали: `console.error([context] ErrorType: message)`
    - Вызывает HapticFeedback error
    - Игнорирует AbortError (не показывает popup)
    - Поддерживает автоматический retry для retryable запросов (1 раз)
    - Показывает popup через showErrorPopup() если showError: true
    - Есть JSDoc комментарий
  - **Files**: `app.js:343`
  - **Estimate**: 30 мин

- [x] 8. Создать главную функцию fetchWithErrorHandling(url, options, config)
  - **Acceptance**:
    - Создаёт AbortController для каждого запроса
    - Устанавливает timeout через setTimeout (default 10s)
    - Выполняет fetch с signal
    - При timeout отменяет запрос и throws TimeoutError
    - При response.ok возвращает response
    - При ошибке вызывает handleNetworkError()
    - Очищает timeout через clearTimeout
    - Есть JSDoc комментарий
  - **Files**: `app.js:418`
  - **Estimate**: 35 мин

---

### Рефакторинг: Интеграция с существующими функциями

- [x] 9. Рефакторить loadAvailableDates() на использование fetchWithErrorHandling
  - **Acceptance**:
    - Использует fetchWithErrorHandling через BookingAPI.getAvailableDates
    - Передаёт context: 'get_available_dates'
    - Передаёт retryable: true (GET запрос)
    - Старый код обработки ошибок удалён
    - Функциональность не изменилась
  - **Files**: `app.js:927` (вызывает BookingAPI:84-196)
  - **Estimate**: 20 мин

- [x] 10. Рефакторить loadTimeSlots() на использование fetchWithErrorHandling
  - **Acceptance**:
    - Использует fetchWithErrorHandling через BookingAPI.getAvailableSlots
    - Передаёт context: 'get_slots'
    - Передаёт retryable: true (GET запрос)
    - Старый код обработки ошибок удалён
  - **Files**: `app.js:955` (вызывает BookingAPI:84-196)
  - **Estimate**: 15 мин

- [x] 11. Рефакторить loadUserBookings() на использование fetchWithErrorHandling
  - **Acceptance**:
    - Использует fetchWithErrorHandling через BookingAPI.getUserBookings
    - Передаёт context: 'get_user_bookings'
    - Передаёт retryable: true (GET запрос)
    - Старый код обработки ошибок удалён
  - **Files**: `app.js:1005` (вызывает BookingAPI:84-196)
  - **Estimate**: 15 мин

- [x] 12. Рефакторить createBooking() на использование fetchWithErrorHandling
  - **Acceptance**:
    - Использует fetchWithErrorHandling через BookingAPI.bookSlot
    - Передаёт context: 'book_slot'
    - Передаёт retryable: false (POST запрос - нельзя auto-retry)
    - Manual retry доступен через кнопку в popup
    - Старый код обработки ошибок удалён
  - **Files**: `app.js:882` (вызывает BookingAPI:84-196)
  - **Estimate**: 20 мин

- [x] 13. Рефакторить остальные API функции (cancel/update booking, etc)
  - **Acceptance**:
    - Все функции с fetch к Make.com используют fetchWithErrorHandling
    - POST/PUT/DELETE запросы имеют retryable: false
    - GET запросы имеют retryable: true
    - Контекст указан для каждого запроса
  - **Files**: `app.js:1043` (cancelBooking вызывает BookingAPI:84-196)
  - **Estimate**: 25 мин

---

### HOTFIX: Исправление критических багов

**Контекст**: После анализа логов (LogIos/Логи31.01v5.txt) выявлены критические проблемы:
- Необработанные Promise rejections
- Флаг `State.isPopupOpen` не сбрасывается корректно
- Race condition при множественных ошибках

- [x] 13.1. Исправить showErrorPopup() - добавить fallback сброс флага и return boolean
  - **Acceptance**:
    - Функция возвращает `true` если popup показан, `false` если нет
    - Добавлен fallback `setTimeout(() => State.isPopupOpen = false, 5000)` для гарантированного сброса
    - Callback `tg.showPopup()` всегда сбрасывает флаг
    - JSDoc обновлён с описанием return значения
  - **Files**: `app.js:245-285`
  - **Estimate**: 10 мин

- [x] 13.2. Исправить handleNetworkError() - не бросать ошибку если popup не показан
  - **Acceptance**:
    - Проверяет результат `showErrorPopup()` (true/false)
    - Если popup НЕ показан (false), логирует warning и НЕ выбрасывает ошибку
    - Если popup показан (true), работает как раньше
    - Добавлен комментарий объясняющий логику
  - **Files**: `app.js:362-424`
  - **Estimate**: 15 мин

- [x] 13.3. Улучшить обработку ошибок в loadAvailableDates()
  - **Acceptance**:
    - Catch блок НЕ пробрасывает ошибку если она уже обработана (есть popup)
    - Логирует только если это новая необработанная ошибка
    - Рендерит пустой календарь при ошибке (не ломает UI)
  - **Files**: `app.js:964-990`
  - **Estimate**: 10 мин

- [x] 13.4. Улучшить обработку ошибок в loadUserBookings()
  - **Acceptance**:
    - Catch блок обрабатывает все типы ошибок корректно
    - Не показывает дублирующий alert если уже показан popup
    - Рендерит пустой список бронирований при ошибке
  - **Files**: `app.js:1035-1072`
  - **Estimate**: 10 мин

- [x] 13.5. Добавить глобальный обработчик unhandledrejection
  - **Acceptance**:
    - В index.html добавлен `window.addEventListener('unhandledrejection')`
    - Логирует необработанные Promise rejections для отладки
    - Показывает fallback сообщение пользователю (если ошибка критичная)
    - Не показывает popup если уже открыт
  - **Files**: `index.html`
  - **Estimate**: 15 мин

---

### Кеширование данных (localStorage)

**⭐ START HERE - начни работу с этого раздела!**

**Контекст**: После анализа логов (LogIos/Логи31.01v6.txt) выявлена проблема:
- Make.com timeout (10+ секунд) блокирует весь UI
- Пользователь ждёт 20+ секунд при retry
- При повторном заходе данные загружаются заново (нет кеша)

**Решение**: Реализовать паттерн "stale-while-revalidate" с кешированием в localStorage

- [x] 19. ⭐ Создать модуль CacheManager для работы с localStorage
  - **Acceptance**:
    - Создан объект `CacheManager` с методами:
      - `set(key, data, ttl)` - сохраняет данные с временем жизни
      - `get(key)` - возвращает `{data, isExpired}` или `null`
      - `clear(key)` - очищает конкретный кеш
      - `clearAll()` - очищает весь кеш приложения
    - Данные сохраняются в формате: `{data: any, timestamp: number, ttl: number}`
    - Проверка срока годности при `get()`
    - Try/catch для обработки QuotaExceededError
    - Логирование операций кеша для отладки
  - **Files**: `app.js` (новый модуль в начале файла, после State)
  - **Estimate**: 30 мин

- [x] 20. Интегрировать кеширование в loadUserBookings()
  - **Acceptance**:
    - При загрузке сначала проверяем кеш `cache_bookings_{user_id}`
    - Если кеш есть и свежий (TTL 5 минут):
      - Показываем кешированные данные мгновенно
      - В фоне делаем запрос к Make.com
      - Обновляем кеш и UI при получении новых данных
    - Если кеша нет или устарел:
      - Показываем loader
      - Делаем запрос к Make.com
      - Сохраняем в кеш
    - При timeout показываем старые данные из кеша (если есть)
    - Логируем: "📦 Загружены из кеша" / "🔄 Обновление из Make.com"
  - **Files**: `app.js:1070-1109` (loadUserBookings)
  - **Estimate**: 40 мин

- [x] 21. Интегрировать кеширование в loadAvailableDates()
  - **Acceptance**:
    - Кеш по ключу `cache_dates_{service_name}`
    - TTL: 10 минут
    - Паттерн "stale-while-revalidate" (показываем старые данные → обновляем в фоне)
    - При timeout показываем кешированные даты с индикатором "Данные могут быть устаревшими"
    - Логирование операций кеша
  - **Files**: `app.js:1188-1271` (loadAvailableDates, loadAvailableDatesFromAPI)
  - **Estimate**: 40 мин

- [x] 22. Интегрировать кеширование в loadAvailableSlots()
  - **Acceptance**:
    - Кеш по ключу `cache_slots_{service}_{date}`
    - TTL: 10 минут
    - Паттерн "stale-while-revalidate"
    - При timeout показываем кешированные слоты
    - Логирование операций кеша
  - **Files**: `app.js:1274-1366` (loadAvailableSlots, loadAvailableSlotsFromAPI)
  - **Estimate**: 40 мин

- [x] 23. Добавить инвалидацию кеша при создании бронирования
  - **Acceptance**:
    - После успешного создания бронирования:
      - Очистить `cache_bookings_{user_id}` (список бронирований изменился)
      - Очистить `cache_dates_*` (доступные даты могли измениться)
      - Очистить `cache_slots_*` (слоты изменились)
    - Логировать инвалидацию: "🗑️ Кеш инвалидирован после создания бронирования"
  - **Files**: `app.js:1154-1160` (confirmBooking)
  - **Estimate**: 20 мин

- [x] 24. Добавить инвалидацию кеша при отмене бронирования
  - **Acceptance**:
    - После успешной отмены бронирования:
      - Очистить `cache_bookings_{user_id}`
      - Очистить `cache_dates_*`
      - Очистить `cache_slots_*`
    - Логировать инвалидацию: "🗑️ Кеш инвалидирован после отмены бронирования"
  - **Files**: `app.js:1501-1507` (cancelBooking)
  - **Estimate**: 15 мин

---

### Тестирование

- [ ] 14. Протестировать Network Error (нет интернета)
  - **Acceptance**:
    - Отключить WiFi/мобильные данные
    - Попытаться загрузить даты
    - Ожидается popup: "Проверьте интернет-соединение"
    - Кнопка "Повторить" работает
    - HapticFeedback error срабатывает
    - Логи содержат: `[get_available_dates] NETWORK error`
  - **Files**: Тестирование
  - **Estimate**: 15 мин

- [ ] 15. Протестировать Timeout Error (медленная сеть)
  - **Acceptance**:
    - Имитировать медленный ответ (>10s) через DevTools throttling
    - Попытаться загрузить даты
    - Через 10 секунд появляется popup: "Сервер не отвечает. Попробуйте позже"
    - Кнопка "Повторить" работает
  - **Files**: Тестирование
  - **Estimate**: 15 мин

- [ ] 16. Протестировать автоматический retry
  - **Acceptance**:
    - Имитировать кратковременный сбой сети
    - Попытаться загрузить даты
    - Показывается "Повторная попытка..."
    - Через 2 секунды retry выполняется
    - Если успешен - данные загружены
    - Если failed - popup с кнопкой "Повторить"
  - **Files**: Тестирование
  - **Estimate**: 20 мин

- [ ] 17. Протестировать быстрое переключение табов (AbortError)
  - **Acceptance**:
    - Быстро переключать табы 10+ раз
    - AbortError не показывает popup
    - В логах: "Request cancelled"
    - Только последний запрос выполняется
    - Нет зависаний/лагов
  - **Files**: Тестирование
  - **Estimate**: 10 мин

- [ ] 18. Протестировать на мобильном устройстве (iOS/Android)
  - **Acceptance**:
    - Все типы ошибок корректно обрабатываются
    - Popup отображается корректно в Telegram WebApp
    - Retry indicator отображается корректно
    - HapticFeedback работает
    - Touch события работают
  - **Files**: Тестирование
  - **Estimate**: 25 мин

- [ ] 25. Протестировать кеширование данных
  - **Acceptance**:
    - Первый заход: данные загружаются от Make.com, сохраняются в кеш
    - Повторный заход: данные показываются мгновенно из кеша
    - При timeout показываются старые данные из кеша (не пустой экран)
    - После создания бронирования кеш инвалидируется
    - localStorage корректно очищается при clearAll()
    - В логах: "📦 Загружены из кеша" / "🔄 Обновление из Make.com"
  - **Files**: Тестирование
  - **Estimate**: 30 мин

- [ ] 26. Протестировать переполнение localStorage (QuotaExceededError)
  - **Acceptance**:
    - Заполнить localStorage до предела
    - Попытаться сохранить новые данные
    - Ожидается graceful degradation (работа без кеша)
    - Логируется warning: "⚠️ localStorage переполнен"
    - Приложение продолжает работать
  - **Files**: Тестирование
  - **Estimate**: 20 мин

---

## 📝 Примечания по задачам

### Важно

**Безопасность**: В задаче #4 обязательно использовать `textContent` вместо `innerHTML` для предотвращения XSS.

**Retry стратегия**:
- GET запросы (read): автоматический retry 1 раз + manual retry
- POST/PUT/DELETE запросы (write): только manual retry через кнопку

**Логирование**: Не логировать чувствительные данные (user_id, personal info), только технические детали.

**Кеширование** (задачи 19-24):
- TTL для бронирований: 5 минут
- TTL для дат/слотов: 10 минут
- Ключи кеша: `cache_bookings_{user_id}`, `cache_dates_{service}`, `cache_slots_{service}_{date}`
- Паттерн: stale-while-revalidate (показываем старое → обновляем в фоне)
- Обработка QuotaExceededError (localStorage переполнен)
- Инвалидация при мутациях (создание/отмена бронирования)

---

## 🔄 История выполнения

| Дата | Задача | Комментарий |
|------|--------|-------------|
| 2026-01-31 | 1-13 | Core функции обработки ошибок и рефакторинг API функций выполнены в предыдущем чате |
| 2026-02-01 | Анализ | Обнаружены критические баги: unhandled rejections, race condition с popup |
| 2026-02-01 | 13.1-13.5 | Добавлены HOTFIX задачи для исправления критических багов |
| 2026-02-01 | 13.1-13.5 | Все HOTFIX задачи выполнены - критические баги исправлены |
| 2026-02-01 | Анализ v6 | Обнаружена проблема Make.com timeout (20+ секунд блокировки UI) |
| 2026-02-01 | 19-24 | Добавлены задачи для кеширования данных в localStorage |
| 2026-02-01 | 19 | Создан модуль CacheManager с методами set/get/clear/clearAll/clearPattern |
| 2026-02-01 | 20-22 | Интегрировано кеширование в loadUserBookings, loadAvailableDates, loadAvailableSlots |
| 2026-02-01 | 23-24 | Добавлена инвалидация кеша при создании/отмене бронирования |
| 2026-02-01 | HOTFIX | Исправлена ошибка загрузки слотов времени (удалены вызовы несуществующей функции renderTimeSlots) - v14.0 |
| 2026-02-01 | HOTFIX | Исправлен race condition: запросы разных типов (get_slots, get_available_dates) больше не отменяют друг друга - v15.0 |

---

## ⚠️ Проблемы и блокеры

### Активные проблемы

**Проблема #4**: Make.com timeout блокирует UI на 20+ секунд
- **Описание**: При timeout запроса `get_user_bookings` пользователь ждёт 20 секунд (10 сек основной запрос + 10 сек retry), UI полностью заблокирован
- **Логи**: `LogIos/Логи31.01v6.txt:line 89-102` - двойной timeout
- **Решение**: Задачи 19-24 (кеширование в localStorage)
- **Приоритет**: HIGH

**Проблема #5**: Нет кеширования - повторная загрузка данных при каждом заходе
- **Описание**: При переходе между табами данные загружаются заново от Make.com, нет использования кеша
- **Решение**: Задачи 19-24 (паттерн stale-while-revalidate)
- **Приоритет**: MEDIUM

### Решённые проблемы

**✅ Проблема #1**: Необработанные Promise rejections - РЕШЕНА (задача 13.2)
- Решение: handleNetworkError теперь не выбрасывает ошибку если popup не показан

**✅ Проблема #2**: Race condition с popup - РЕШЕНА (задачи 13.1, 13.2)
- Решение: showErrorPopup возвращает boolean, fallback timeout сбрасывает флаг

**✅ Проблема #3**: Флаг isPopupOpen не сбрасывается - РЕШЕНА (задача 13.1)
- Решение: Добавлен fallback setTimeout на 5 секунд для гарантированного сброса

**✅ Проблема #4**: Дублирующие алерты - РЕШЕНА (задачи 13.3, 13.4)
- Решение: Удалены дублирующие tg.showAlert из loadAvailableDates и loadUserBookings

**✅ Проблема #5**: Глобальные unhandled rejections - РЕШЕНА (задача 13.5)
- Решение: Улучшен обработчик unhandledrejection с fallback popup для пользователя

**✅ Проблема #6**: Ошибка загрузки слотов времени - РЕШЕНА (HOTFIX v14.0)
- Описание: При выборе даты появлялась ошибка "Не удалось загрузить доступные слоты", хотя данные загружались успешно
- Причина: Вызовы несуществующей функции renderTimeSlots() в loadAvailableSlots() и loadAvailableSlotsFromAPI()
- Логи: `LogIos/Логи01.02v7.txt:line 204,526` - ReferenceError при вызове renderTimeSlots
- Решение: Удалены все вызовы renderTimeSlots(), UI обновляется через renderBookingScreen() в selectDate()

**✅ Проблема #7**: Race condition при загрузке слотов - РЕШЕНА (HOTFIX v15.0)
- Описание: При выборе даты появлялось сообщение "Повторная загрузка" или "Ошибка сервера", затем слоты подгружались с задержкой
- Причина: Единый `State.currentRequest` для всех запросов - `get_slots` отменял `get_available_dates` и наоборот
- Логи: `LogIos/Логи01.02v8.txt:line 2154-2243` - взаимная отмена запросов разных типов
- Решение: Заменён `State.currentRequest` на `State.requestControllers` (Map по context), запросы одного типа отменяют только предыдущие запросы того же типа

---

## ✅ Критерии завершения тикета

Тикет считается завершённым когда:

- [ ] Все задачи в tasklist выполнены (`[x]`)
- [ ] Код прошёл самостоятельное ревью
- [ ] Все тесты пройдены (manual testing)
- [ ] Нет ошибок в консоли
- [ ] Нет известных critical багов
- [ ] CHANGELOG обновлён
- [ ] Код закоммичен
- [ ] Статус изменён на `COMPLETED`

---

## 💡 Советы по работе с tasklist

1. **Работай последовательно** - выполняй задачи в порядке 1→2→3...
2. **Коммить часто** - можно после каждой 2-3 задач
3. **Отмечай прогресс** - ставь `[x]` сразу после выполнения
4. **Тестируй по ходу** - не оставляй все тесты на конец
5. **Обновляй прогресс** - меняй числа в разделе "📊 Прогресс"
