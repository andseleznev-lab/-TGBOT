# Tasklist: T-012 — Email для фискального чека YooKassa

**Тикет**: T-012
**PRD**: `docs/prd/T-012.prd.md`
**План**: `docs/plan/T-012.md`
**Дата создания**: 2026-03-01
**Статус**: 🚧 IN PROGRESS

---

## 📊 Прогресс: 5/6 задач (83%)

```
[█████████████████   ] 83%
```

---

## 🗂️ Задача 1: State и инициализация (app.js)

- [x] Добавить `userEmail: null` в объект `State`
- [x] При старте приложения читать `localStorage.getItem('user_email')` → `State.userEmail`

---

## 🎨 Задача 2: UI поле email в renderBookingScreen() (app.js)

- [x] Определить `isPaidService` (price > 0) в `renderBookingScreen()`
- [x] Добавить блок `.email-input-section` с `<input type="email">` между слотами и кнопкой подтверждения
- [x] Показывать блок только если `State.selectedSlot && isPaidService`
- [x] Pre-fill из `escapeHtml(State.userEmail || '')`
- [x] `oninput` обновляет `State.userEmail = this.value.trim()`

---

## ✅ Задача 3: Валидация email в confirmBooking() (app.js)

- [x] Для платных услуг: проверить что `State.userEmail` непустой
- [x] Проверить формат через regex `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
- [x] При ошибке: `HapticFeedback.notificationOccurred('error')` + `showSlotTakenPopup()`
- [x] При успехе: `localStorage.setItem('user_email', email)` → передать в `createPayment()`

---

## 🔌 Задача 4: Передача email в createPayment() (app.js)

- [x] Добавить параметр `email` в сигнатуру `createPayment(slotId, serviceId, email)`
- [x] Добавить `user_email: email` в payload `BookingAPI.request('create_payment', {...})`
- [x] Обновить вызов из `confirmBooking()`: передать `email`

---

## 🎨 Задача 5: Стили (styles.css)

- [x] Добавить `.email-input-section`, `.email-label`, `.email-input`, `.email-input:focus`

---

## 🔧 Задача 6: Make.com (вручную)

- [ ] Добавить блок `receipt` в HTTP body к YooKassa:
  - `customer.email: {{metadata.user_email}}`
  - `items[]` с description, quantity, amount, vat_code
- [ ] Проверить `vat_code` в настройках YooKassa ЛК

---

## Зависимости

- `escapeHtml()` — уже есть в app.js
- `showSlotTakenPopup()` — уже есть в app.js
- CSS-переменные `--text-secondary`, `--text-primary` — уже есть в styles.css
