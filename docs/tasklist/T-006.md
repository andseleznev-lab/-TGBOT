# Tasklist: T-006 - Supabase PostgreSQL + Realtime

**Тикет**: T-006
**PRD**: `docs/prd/T-006.prd.md`
**План**: `docs/plan/T-006.md`
**Дата создания**: 2026-02-15
**Статус**: IN PROGRESS

---

## 📊 Прогресс: 18/22 задач (82%)

```
[████████████████░░░░] 82%
```

---

## ✅ Подготовка (2/2)

- [x] Создать таблицу `club_payments` в Supabase
  - SQL скрипт: `docs/supabase-setup.sql`
  - RLS политики настроены
  - Индексы созданы
  - Realtime включён

- [x] Протестировать Supabase REST API
  - SELECT работает с anon ключом ✅
  - INSERT блокируется с anon ключом ✅
  - RLS политики работают корректно ✅

---

## 🎨 Фаза 1: Подключение Supabase SDK (3/3) ✅

- [x] Добавить Supabase SDK в `index.html`
  - Подключить CDN: `@supabase/supabase-js@2.39.3`
  - Разместить перед `config.js`
  - Обновить версии: `config.js?v=28`, `app.js?v=43`

- [x] Добавить конфигурацию в `config.js`
  - `SUPABASE.URL: 'https://ujldixoiaqybtnifzhgy.supabase.co'`
  - `SUPABASE.ANON_KEY: 'eyJhbGc...'` (полный ключ)
  - `SUPABASE.ENABLED: true` (флаг миграции)
  - `CLUB.ZOOM_LINK` добавлен

- [x] Создать `initSupabase()` в `app.js`
  - Проверка `CONFIG.SUPABASE.ENABLED`
  - Проверка загрузки `window.supabase`
  - Создание клиента: `createClient(URL, KEY)`
  - Логирование подключения
  - Обработка ошибок
  - Вызван в `initApp()`

---

## 📥 Фаза 2: Загрузка платежей (3/3) ✅

- [x] Создать `loadUserPayments(userId)` в `app.js`
  - SELECT из `club_payments` ✅
  - Фильтр: `.eq('telegram_user_id', userId)` ✅
  - Фильтр: `.eq('status', 'succeeded')` ✅
  - Сортировка: `.order('created_at', { ascending: false })` ✅
  - Обработка ошибок ✅
  - Возврат массива платежей ✅

- [x] Создать `calculateMeetings(paymentDate)` в `app.js`
  - Парсинг даты (string или Date) ✅
  - Использует `getNextSundays()` для вычисления воскресений ✅
  - Форматирование дат: "DD.MM.YYYY" ✅
  - Возврат массива: `[{date, formatted, time}]` ✅

- [x] Интегрировать в `loadClubData()`
  - Проверка `CONFIG.SUPABASE.ENABLED && supabaseClient` ✅
  - Если `true`: вызвать `loadUserPayments(userId)` ✅
  - Трансформация данных: `paid_at`, `user_id` алиасы ✅
  - Если `false` или ошибка: fallback на `club.json` ✅
  - Сохранить в `State.clubPayments` ✅

---

## 🔌 Фаза 3: Realtime подписка (5/5) ✅

- [x] Создать `subscribeToPayments(userId)` в `app.js`
  - Проверка `CONFIG.SUPABASE.ENABLED` ✅
  - Отписка от предыдущей подписки (если есть) ✅
  - Создание канала: `client.channel('club-payments')` ✅
  - Подписка на `postgres_changes` (event: INSERT) ✅
  - Обработка статусов: SUBSCRIBED, CHANNEL_ERROR, TIMED_OUT ✅
  - Логирование событий ✅

- [x] Создать `handleRealtimePayment(payment, userId)` в `app.js`
  - Проверка: `payment.telegram_user_id === userId` ✅
  - Проверка: `payment.status === 'succeeded'` ✅
  - Haptic feedback: `notificationOccurred('success')` ✅
  - Показ popup: `showSuccessPopup('✅ Абонемент куплен!')` ✅
  - Вызов `updateClubUIAfterPayment(payment)` ✅

- [x] Создать `updateClubUIAfterPayment(payment)` в `app.js`
  - Добавить платёж в `State.clubPayments` ✅
  - Трансформация: `paid_at`, `user_id` алиасы ✅
  - Проверка: `State.currentTab === 'club'` ✅
  - Если да: вызвать `renderClubScreen()` (обновить UI) ✅
  - Логирование обновления ✅

- [x] Добавить подписку в `switchTab('club')`
  - Вызвать `subscribeToPayments(userId)` при открытии вкладки ✅
  - Логирование активации подписки ✅

- [x] Добавить отписку при переключении вкладок
  - В `switchTab(otherTab)` проверить `realtimeSubscription` ✅
  - Если есть: вызвать `realtimeSubscription.unsubscribe()` ✅
  - Установить `realtimeSubscription = null` ✅

---

## 🧹 Фаза 4: Cleanup (1/1) ✅

- [x] Добавить cleanup при закрытии приложения
  - `window.addEventListener('beforeunload', ...)` ✅
  - Отписка от Realtime: `realtimeSubscription?.unsubscribe()` ✅
  - Логирование cleanup ✅

---

## 🎨 Фаза 5: UI обновления (2/2) ✅

- [x] `renderClubScreen()` уже работает корректно
  - Использует `State.clubPayments` с трансформированными данными ✅
  - Отображает Zoom ссылку из `State.clubZoomLink` ✅
  - Вызывает `getNextSundays(paidDate)` для вычисления встреч ✅
  - Кнопка "Перейти в Zoom" открывает `CONFIG.CLUB.ZOOM_LINK` ✅

- [x] `showSuccessPopup(title, message)` уже существует
  - Haptic feedback при показе ✅
  - Кастомный HTML popup из index.html ✅
  - Стилизация согласно дизайну приложения ✅
  - Используется в `handleRealtimePayment()` ✅

---

## 🔧 Фаза 6: Make.com Backend (2/3)

**Делает пользователь** (не Claude):

- [x] Найти WEBHOOK 2 в Make.com (YooKassa succeeded)
  - Сценарий: "YooKassa → Make.com webhook" ✅
  - Найти модуль обновления `club.json` ✅

- [x] Заменить на HTTP Request → Supabase
  - Method: POST ✅
  - URL: `https://ujldixoiaqybtnifzhgy.supabase.co/rest/v1/club_payments` ✅
  - Headers: `apikey` (service_role), `Authorization: Bearer`, `Content-Type`, `Prefer` ✅
  - Body: `{ payment_id, telegram_user_id, amount, status, payment_data }` ✅
  - Используется `service_role` ключ из Environment Variables ✅
  - Протестировано: HTTP 201 Created ✅

- [ ] Добавить Error Handler в Make.com
  - Если HTTP status !== 201
  - Действие: Send Email (уведомление админу)
  - Логирование в Make.com History

---

## 🧪 Фаза 7: Тестирование (0/5)

- [ ] Unit тест: `loadUserPayments()`
  - Тест с существующим userId → вернёт массив
  - Тест с несуществующим userId → вернёт `[]`
  - Тест при `USE_SUPABASE = false` → вернёт `[]` + warning

- [ ] Unit тест: `calculateMeetings()`
  - Тест с датой 15.02.2026 → вернёт 4 воскресенья
  - Проверка формата дат: "DD.MM.YYYY"
  - Проверка что интервал = 7 дней

- [ ] Realtime тест: Симуляция INSERT
  - Вставить платёж через Supabase Dashboard (SQL Editor)
  - `INSERT INTO club_payments (...) VALUES (userId текущего, ...)`
  - Проверка: Haptic + Popup + UI update < 1 сек

- [ ] Realtime тест: Событие для другого пользователя
  - Вставить платёж для `userId = 999999999`
  - Проверка: НЕ должно показать popup (логирование в консоли)

- [ ] End-to-End тест: Реальная оплата
  - Нажать "Купить абонемент 2990₽"
  - Оплатить тестовой картой YooKassa (4111 1111 1111 1111)
  - Проверка Make.com History: HTTP 201 от Supabase
  - Проверка фронт: Popup + 4 даты встреч < 1 сек

---

## 🔐 Фаза 8: Security Review (0/2)

- [ ] Проверить что `service_role` ключ НЕ в config.js
  - Grep по коду: `service_role`
  - Убедиться что только в Make.com Environment Variables

- [ ] Проверить RLS политики через DevTools
  - Попытка INSERT с anon ключом → должна провалиться
  - Попытка UPDATE с anon ключом → должна провалиться
  - SELECT pending платежей → должен вернуть `[]`

---

## 🚀 Фаза 9: Деплой (0/1)

- [ ] Деплой на GitHub Pages
  - Commit: `[T-006] Supabase PostgreSQL + Realtime интеграция`
  - Push в `main` ветку
  - Проверка деплоя: ~2 минуты
  - Тестирование в production

---

## 📊 Метрики успеха

После завершения всех задач проверить:

- [ ] Время от оплаты до UI update: **< 1 секунда** ✅
- [ ] WebSocket reconnect (Airplane Mode test): **< 3 секунды** ✅
- [ ] Bundle size увеличение: **~50KB** (Supabase SDK) ✅
- [ ] API запросов на загрузку вкладки "Клуб": **1 запрос** (было 50+) ✅

---

## 🎯 Критерии завершения T-006

Тикет считается завершённым когда:

- [ ] Все задачи отмечены `[x]`
- [ ] Realtime работает стабильно
- [ ] Make.com webhook интегрирован
- [ ] Security review пройден
- [ ] Production деплой выполнен
- [ ] Метрики успеха достигнуты

---

**Обновлено**: 2026-02-15
**Следующая задача**: Добавить Supabase SDK в `index.html`
