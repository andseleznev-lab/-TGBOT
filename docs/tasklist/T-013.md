# Tasklist: T-013 — Динамический Zoom-link клуба, уникальные ссылки per user

**Тикет**: T-013
**PRD**: `docs/prd/T-013.prd.md`
**План**: `docs/plan/T-013.md`
**Дата создания**: 2026-03-02
**Статус**: 🔲 IN PROGRESS

---

## 📊 Прогресс: 5/6 задач (83%)

```
[████████████████    ] 83%
```

---

## 🗃️ Задача 1: Миграция БД — создать club_meetings и club_registrations ✅

- [x] Создать `docs/supabase-migration-club-meetings.sql` (две таблицы + RLS + индексы)
- [x] Удалить устаревший `docs/supabase-migration-add-meeting-date.sql`
- [x] Выполнить миграцию через Supabase Management API
- [x] Проверить: обе таблицы доступны для SELECT с anon ключом (RLS политики подтверждены)

---

## ⚙️ Задача 2: app.js — State и loadClubData() ✅

- [x] Добавить `clubMeetings: []` и `clubRegistrations: {}` в объект `State` (строка ~733)
- [x] Оставить `clubZoomLink: ''` как legacy (не используется для новой логики)
- [x] В `loadClubData()` (Supabase-ветка): добавить 2 параллельных запроса (Promise.all):
  - `club_meetings` WHERE `meeting_date >= -28 days`
  - `club_registrations` WHERE `telegram_user_id = USER.id`
- [x] Сохранить результаты в `State.clubMeetings` и `State.clubRegistrations` (map: meeting_id → url)

---

## 🔄 Задача 3: app.js — getNextSundays → getNextWednesdays ✅

- [x] Переименовать функцию `getNextSundays` → `getNextWednesdays`
- [x] Изменить логику: день недели 0 (воскресенье) → 3 (среда)
- [x] Обновить все вызовы `getNextSundays(` → `getNextWednesdays(` (3 вызова)
- [x] Обновить JSDoc функции

---

## 🎨 Задача 4: app.js — renderClubScreen() и renderClubMeetingCard() ✅

**renderClubScreen():**
- [x] Использовать `State.clubMeetings` как источник дат встреч
- [x] Fallback: если `State.clubMeetings` пустой → `getNextWednesdays(paidDate, CONFIG.CLUB.MEETINGS_COUNT)`
- [x] Для каждой встречи: lookup `zoom_join_url` из `State.clubRegistrations` по `meeting_id`
- [x] Передавать `zoomLink: url || null` в `renderClubMeetingCard()`

**renderClubMeetingCard():**
- [x] Убрать обязательную валидацию `zoomLink` (блок с `return ''`)
- [x] Добавить третий вариант footer: `!isPast && !zoomLink` → «Ссылка появится позже» (opacity: 0.5)

---

## 📝 Задача 5: Строки «воскресенье» → «среда» ✅

- [x] `config.js` строка 23: комментарий «каждое воскресенье» → «каждую среду»
- [x] `app.js`: заменить все вхождения «каждое воскресенье в» → «каждую среду в» (5 мест)
- [x] `app.js`: «4 встречи по воскресеньям» → «4 встречи по средам»
- [x] `app.js`: «воскресенья в» → «встречи в» (на экране оплаты)

---

## 🔧 Задача 6: Make.com (вручную)

- [ ] Создать новый сценарий «Клуб: еженедельный Zoom»:
  - Schedule trigger: каждый понедельник 10:00 МСК
  - Zoom Create Meeting → ближайшая среда 19:00 МСК
  - INSERT club_meetings (ON CONFLICT merge)
  - SELECT всех succeeded участников из club_payments
  - Iterator → Zoom Add Registrant → уникальный join_url
  - INSERT club_registrations (ON CONFLICT merge)
- [ ] Обновить существующий payment-сценарий:
  - После succeeded платежа → SELECT ближайшая встреча из club_meetings
  - Zoom Add Registrant для нового участника
  - INSERT club_registrations

---

## Зависимости

- Задача 1 (миграция) — перед тестированием задач 2–4
- Задачи 2, 3, 4, 5 — независимы, можно делать параллельно
- Задача 6 (Make.com) — после задачи 1 (нужны таблицы в БД)

---

## Тест-кейсы

- [ ] TC-1: Таблицы доступны для SELECT с anon ключом после миграции
- [ ] TC-2: Вручную INSERT в club_meetings + club_registrations → кнопка Zoom в боте
- [ ] TC-3: Нет записи в club_registrations → «Ссылка появится позже»
- [ ] TC-4: `State.clubMeetings` пустой → fallback на расчётные среды
- [ ] TC-5: Прошедшая встреча → «Встреча завершена»
- [ ] TC-6: Дубль INSERT → ON CONFLICT, нет ошибки
- [ ] TC-7: Все строки «воскресенье» заменены в UI
