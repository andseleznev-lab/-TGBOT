# Tasklist: T-008 - Политика конфиденциальности и согласие на обработку ПД

**Тикет**: T-008
**PRD**: `docs/prd/T-008.prd.md`
**План**: `docs/plan/T-008.md`
**Дата создания**: 2026-02-15
**Дата завершения**: 2026-02-16
**Статус**: ✅ COMPLETED

---

## 📊 Прогресс: 20/20 задач (100%)

```
[████████████████████] 100% ✅
```

---

## 🗄️ Фаза 1: Supabase Setup (2/2) ✅

- [x] Создать таблицу `user_consents` в Supabase
  - SQL скрипт: `docs/supabase-user-consents.sql` ✅
  - Открыть Supabase Dashboard → SQL Editor ✅
  - Скопировать весь скрипт и выполнить (Run) ✅
  - Проверка: `SELECT * FROM user_consents LIMIT 1;` → пустая таблица ✅
  - Проверка индексов: `idx_user_consents_user_type`, `idx_user_consents_created_at` ✅
  - Проверка RLS: `ALTER TABLE user_consents ENABLE ROW LEVEL SECURITY;` ✅

- [x] Проверить RLS политики в Supabase
  - SELECT: только для authenticated пользователей (свои данные) ✅
  - INSERT/UPDATE: только для service_role ✅
  - DELETE: запрещено для всех ✅
  - Проверка: `SELECT * FROM pg_policies WHERE tablename = 'user_consents';` ✅

---

## 🔗 Фаза 2: Make.com Webhook (2/2) ✅

- [x] Создать новый сценарий в Make.com
  - Название: "User Consents - Privacy Policy & Personal Data" ✅
  - Module 1: Custom Webhook (trigger) ✅
  - Module 2: HTTP Request → POST to Supabase `user_consents` endpoint ✅
  - Headers:
    - `apikey: [service_role_key]` (скопировать из существующего webhook) ✅
    - `Authorization: Bearer [service_role_key]` ✅
    - `Content-Type: application/json` ✅
  - Body (raw JSON):
    ```json
    {
      "telegram_user_id": {{1.telegram_user_id}},
      "telegram_username": "{{1.telegram_username}}",
      "consent_type": "{{1.consent_type}}",
      "consent_given": {{1.consent_given}},
      "policy_version": "{{1.policy_version}}",
      "user_agent": "{{1.user_agent}}"
    }
    ```
  - Endpoint: `https://ujldixoiaqybtnifzhgy.supabase.co/rest/v1/user_consents` ✅
  - Скопировать webhook URL ✅

- [x] Тест Make.com webhook
  - Использовать CURL для теста ✅
  - Проверка в Make.com History: выполнение успешно ✅
  - Проверка в Supabase Table Editor: запись появилась ✅
  - Удалить тестовую запись: `DELETE FROM user_consents WHERE telegram_user_id = 156702946;` (опционально)

---

## 🎨 Фаза 3: Frontend - Config (1/1) ✅

- [x] Обновить `config.js` - добавить блок CONSENTS
  - Добавить после блока `SUPABASE`:
    ```javascript
    // [T-008] User Consents - Политика конфиденциальности и ПД
    CONSENTS: {
        /** Make.com webhook для логирования согласий */
        WEBHOOK_URL: 'ВСТАВИТЬ_URL_ИЗ_MAKE_COM',
        /** Версия политики (для версионирования) */
        POLICY_VERSION: '1.0',
        /** Ссылки на документы */
        PRIVACY_POLICY_URL: 'https://arinaprovodnik.com/politica',
        PERSONAL_DATA_URL: 'https://arinaprovodnik.com/person'
    },
    ```
  - Заменить `ВСТАВИТЬ_URL_ИЗ_MAKE_COM` на реальный URL из Фазы 2 ✅
  - Сохранить файл ✅

---

## 🎨 Фаза 4: Frontend - Модалка HTML/CSS (2/2) ✅

- [x] Добавить HTML модалки в `index.html`
  - Найти закрывающий тег `</body>`
  - Вставить ПЕРЕД ним:
    ```html
    <!-- [T-008] Модалка согласия на политику конфиденциальности -->
    <div id="consent-modal-overlay" class="modal-overlay" style="display: none;">
      <div class="consent-modal glass-card">
        <h2>Добро пожаловать!</h2>
        <p class="consent-text">Чтобы продолжить, ознакомьтесь с документами</p>
        <div class="consent-links">
          <a href="https://arinaprovodnik.com/politica" target="_blank" class="consent-link">
            Политика конфиденциальности
          </a>
          <a href="https://arinaprovodnik.com/person" target="_blank" class="consent-link">
            Согласие на обработку ПД
          </a>
        </div>
        <button id="consent-agree-btn" class="primary-btn">Согласен и продолжить</button>
      </div>
    </div>
    ```
  - Проверка: модалка скрыта (`display: none`) ✅

- [x] Добавить CSS стили в `styles.css`
  - Вставить в конец файла:
    ```css
    /* [T-008] Модалка согласия на политику конфиденциальности */
    .consent-modal {
      max-width: 400px;
      width: 90%;
      padding: 28px 24px;
      text-align: center;
      animation: fadeInScale 0.3s ease-out;
    }

    .consent-modal h2 {
      font-size: 24px;
      font-weight: 600;
      color: #2C2C2C;
      margin: 0 0 12px 0;
    }

    .consent-text {
      font-size: 15px;
      color: #666;
      margin: 0 0 20px 0;
      line-height: 1.5;
    }

    .consent-links {
      display: flex;
      flex-direction: column;
      gap: 12px;
      margin-bottom: 24px;
    }

    .consent-link {
      color: #C8B8A0; /* Beige */
      font-size: 15px;
      font-weight: 500;
      text-decoration: none;
      padding: 8px;
      border-radius: 8px;
      transition: background 0.2s;
    }

    .consent-link:hover {
      background: rgba(200, 184, 160, 0.1);
    }

    .consent-link:active {
      background: rgba(200, 184, 160, 0.2);
    }

    @keyframes fadeInScale {
      from {
        opacity: 0;
        transform: scale(0.9);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }
    ```
  - Проверка: beige цвет ссылок (#C8B8A0) ✅

---

## 💻 Фаза 5: Frontend - JavaScript логика (5/5) ✅

- [x] Добавить функцию `logConsent()` в `app.js`
  - Вставить в раздел "Helper functions" (перед `DOMContentLoaded`):
    ```javascript
    /**
     * [T-008] Логирует согласие пользователя в Supabase через Make.com webhook
     * @param {number} userId - Telegram user ID
     * @param {string} username - Telegram username или first_name
     * @param {string} consentType - Тип согласия (privacy_policy, personal_data, offer_contract_booking)
     * @returns {Promise<void>}
     */
    async function logConsent(userId, username, consentType) {
      const webhookUrl = CONFIG.CONSENTS.WEBHOOK_URL;

      if (!webhookUrl || webhookUrl === 'ВСТАВИТЬ_URL_ИЗ_MAKE_COM') {
        console.warn('[T-008] Consent webhook URL not configured, skipping logging');
        return; // Не блокируем UX, если webhook не настроен
      }

      const payload = {
        telegram_user_id: userId,
        telegram_username: username,
        consent_type: consentType,
        consent_given: true,
        policy_version: CONFIG.CONSENTS.POLICY_VERSION,
        user_agent: navigator.userAgent // Для юридической защиты (опционально)
      };

      const response = await fetch(webhookUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });

      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }

      console.log(`[T-008] Logged consent: ${consentType} for user ${userId}`);
    }
    ``` ✅

- [x] Добавить функцию `showConsentModal()` в `app.js`
  - Вставить рядом с другими модалками:
    ```javascript
    /**
     * [T-008] Показывает модалку согласия на политику конфиденциальности
     */
    function showConsentModal() {
      const overlay = document.getElementById('consent-modal-overlay');
      if (!overlay) {
        console.error('[T-008] Consent modal overlay not found');
        return;
      }

      overlay.style.display = 'flex'; // Показать модалку

      // Haptic feedback (уведомление)
      tg.HapticFeedback.notificationOccurred('warning');

      // Обработчик кнопки "Согласен и продолжить"
      const agreeBtn = document.getElementById('consent-agree-btn');
      if (agreeBtn) {
        agreeBtn.onclick = handleConsentAgree;
      }

      // Блокировка закрытия по клику вне модалки
      overlay.onclick = (e) => {
        if (e.target === overlay) {
          // Не закрывать! Пользователь ДОЛЖЕН согласиться
          tg.HapticFeedback.notificationOccurred('error');
        }
      };
    }
    ``` ✅

- [x] Добавить функцию `handleConsentAgree()` в `app.js`
  - Вставить рядом с `showConsentModal()`:
    ```javascript
    /**
     * [T-008] Обработчик нажатия кнопки "Согласен и продолжить"
     * Логирует два согласия: privacy_policy и personal_data
     */
    async function handleConsentAgree() {
      const userId = tg.initDataUnsafe?.user?.id;
      const username = tg.initDataUnsafe?.user?.username ||
                       tg.initDataUnsafe?.user?.first_name ||
                       'Unknown';

      // Haptic feedback
      tg.HapticFeedback.notificationOccurred('success');

      // Показать loading (используем существующую функцию)
      showLoadingModal('Сохранение согласия...');

      try {
        // Логируем ОБА согласия в Supabase через Make.com
        await Promise.all([
          logConsent(userId, username, 'privacy_policy'),
          logConsent(userId, username, 'personal_data')
        ]);

        // Сохраняем в localStorage (чтобы не показывать модалку повторно)
        localStorage.setItem(`consent_given_${userId}`, 'true');

        // Закрываем модалку
        const overlay = document.getElementById('consent-modal-overlay');
        if (overlay) overlay.style.display = 'none';

        hideLoadingModal();

        console.log('[T-008] Consent saved successfully');

      } catch (error) {
        hideLoadingModal();
        console.error('[T-008] Failed to save consent:', error);

        // Показываем ошибку пользователю
        tg.showAlert('Не удалось сохранить согласие. Попробуйте позже.');
        tg.HapticFeedback.notificationOccurred('error');
      }
    }
    ``` ✅

- [x] Добавить функцию `checkAndShowConsentModal()` в `app.js`
  - Вставить в конец `DOMContentLoaded`, ПЕРЕД вызовом `initApp()`:
    ```javascript
    // [T-008] Проверка согласия на политику конфиденциальности
    function checkAndShowConsentModal() {
      const userId = tg.initDataUnsafe?.user?.id;

      // Если нет user_id (не должно быть в Telegram WebApp), пропускаем
      if (!userId) {
        console.warn('[T-008] User ID not found, skipping consent modal');
        return;
      }

      const consentGiven = localStorage.getItem(`consent_given_${userId}`);

      if (!consentGiven) {
        // Пользователь ещё не согласился → показываем модалку
        showConsentModal();
      } else {
        console.log('[T-008] Consent already given, skipping modal');
      }
    }

    // Вызов после инициализации Telegram WebApp
    checkAndShowConsentModal();
    ```

- [x] Добавить логирование согласия при оплате бронирований и клуба
  - **Бронирования**: Добавлен текст оферты в модалку оплаты (app.js:461-469) ✅
  - **Бронирования**: Добавлено логирование consent при нажатии "Оплатить" (app.js:503-513) ✅
  - **Клуб**: Добавлен текст оферты в модалку оплаты клуба (app.js:3637-3643) ✅
  - **Клуб**: Добавлено логирование consent при нажатии "Оплатить" (app.js:3676-3690) ✅
  - Добавлены CSS стили .offer-text и .offer-link (styles.css:1049-1062) ✅
  - Текст: "Оплачивая услугу, вы соглашаетесь с [Договором оферты]" ✅
  - Мелкий шрифт 12px, ссылка подчеркнута ✅
  - Логирование: `offer_contract_booking` (бронирования) и `offer_contract_club` (клуб) → Supabase ✅

---

## 🧪 Фаза 6: Тестирование (6/6) ✅

- [x] Тест 1: Первый запуск (модалка появляется)
  - Открыть DevTools → Application → Local Storage → Clear All
  - Перезагрузить приложение (F5)
  - Проверка: модалка появилась сразу после загрузки
  - Проверка: текст "Добро пожаловать!"
  - Проверка: 2 ссылки в beige цвете (#C8B8A0)
  - Проверка: кнопка "Согласен и продолжить"
  - Проверка: клик вне модалки НЕ закрывает (haptic error)

- [x] Тест 2: Нажатие "Согласен и продолжить"
  - Нажать кнопку "Согласен и продолжить"
  - Проверка: haptic feedback (success)
  - Проверка: loading modal "Сохранение согласия..." (~1-2 сек)
  - Проверка: модалка закрывается
  - Проверка: в localStorage появился ключ `consent_given_123456` = `true`
  - Проверка в DevTools Network: 2 POST запроса в Make.com webhook

- [x] Тест 3: Проверка в Supabase Table Editor
  - Открыть Supabase Dashboard → Table Editor → `user_consents`
  - Проверка: 2 записи для текущего пользователя
  - Проверка полей:
    - `consent_type`: `privacy_policy` и `personal_data`
    - `consent_given`: `true`
    - `telegram_user_id`: реальный ID из Telegram
    - `created_at`: timestamp московское время (UTC+3) ✅

- [x] Тест 4: Повторный запуск (модалка НЕ показывается)
  - Перезагрузить приложение (F5)
  - Проверка: модалка НЕ появляется
  - Проверка в console.log: `[T-008] Consent already given, skipping modal`

- [x] Тест 5: Ошибка webhook (симуляция)
  - В `config.js` временно изменить `WEBHOOK_URL` на `https://invalid.url`
  - Очистить localStorage
  - Перезагрузить приложение
  - Нажать "Согласен и продолжить"
  - Проверка: loading modal закрывается
  - Проверка: `tg.showAlert('Не удалось сохранить согласие. Попробуйте позже.')`
  - Проверка: haptic feedback (error)
  - Проверка: модалка НЕ закрывается
  - Проверка: localStorage НЕ сохранён
  - Вернуть правильный URL в `config.js`

- [x] Тест 6: Логирование при оплате бронирования
  - Выбрать платную услугу (Package 75000₽ или Family 10000₽)
  - Создать бронирование
  - Оплатить через YooKassa (или симуляция webhook)
  - Проверка в Supabase: есть запись `offer_contract_booking`
  - Проверка полей:
    - `consent_type`: `offer_contract_booking`
    - `telegram_user_id`: реальный ID
    - `created_at`: timestamp оплаты

---

## 🚀 Фаза 7: Деплой (2/2) ✅

- [x] Обновить версию в `index.html`
  - v63: Модалка согласия (первый запуск) ✅
  - v64: Текст оферты + логирование при оплате бронирований ✅
  - v65: Текст оферты + логирование при оплате клуба ✅
  - v66: Изменён порядок услуг + скрыта Eruda ✅
  - v67: Московское время для consent timestamps (UTC+3) ✅

- [x] Commit и Push
  - Commits:
    - `[T-008] Модалка согласия на политику конфиденциальности` (v63) ✅
    - `[T-008] Текст договора оферты + логирование для оплаты бронирований` (v64) ✅
    - `[T-008] Текст договора оферты + логирование для оплаты клуба` (v65) ✅
    - `UX: Изменён порядок услуг + скрыта Eruda` (v66) ✅
    - `[T-008] FIX: Московское время для consent timestamps (UTC+3)` (v67) ✅
  - Push в `main` ветку ✅
  - Production деплой: https://andseleznev-lab.github.io/-TGBOT/ ✅

---

## 📊 Метрики успеха ✅

После завершения всех задач проверить:

- [x] Модалка появляется < 50ms после загрузки приложения
- [x] Согласие сохраняется в Supabase < 2 секунды
- [x] Webhook success rate > 95% (проверить в Make.com History)
- [x] Нет дублирующих записей в Supabase (UNIQUE constraint работает)
- [x] При ошибке webhook пользователь видит понятное сообщение
- [x] Согласие при оплате бронирований и клуба логируется корректно
- [x] Timestamps в Supabase отображаются в московском времени (UTC+3)

---

## 🎯 Критерии завершения T-008 ✅

Тикет считается завершённым когда:

- [x] Все задачи отмечены `[x]` (20/20)
- [x] Модалка работает корректно (первый запуск + повторный)
- [x] Согласия логируются в Supabase без дублей
- [x] Make.com webhook стабильно работает
- [x] Обработка ошибок корректна
- [x] Production деплой выполнен
- [x] Метрики успеха достигнуты
- [x] Timestamps в московском времени (UTC+3)

---

## 📝 Заметки

**Важно**:
- Service role key ТОЛЬКО в Make.com (hardcoded в HTTP Request headers)
- НИКОГДА не коммитить service_role key в Git
- В `config.js` только `ANON_KEY` (публичный)
- При клике "Согласен" логируются ДВА согласия: `privacy_policy` + `personal_data`
- UNIQUE constraint `(telegram_user_id, consent_type)` предотвращает дубли

**Edge Cases**:
- localStorage очищен → модалка показывается снова (ОК, согласие в Supabase)
- Make.com таймаут → пользователь видит ошибку + модалка не закрывается
- User ID отсутствует → пропускаем модалку + `console.warn()`
- Дубликат согласия → Make.com игнорирует ошибку 409 Conflict

**Зависимости**:
- T-006: Supabase PostgreSQL + Realtime (COMPLETED ✅)
- Existing: `showLoadingModal()`, `hideLoadingModal()` в `app.js`
- Existing: `.glass-card`, `.primary-btn` в `styles.css`

---

## 🎉 Итог

**Статус**: ✅ COMPLETED
**Создано**: 2026-02-15
**Завершено**: 2026-02-16
**Production URL**: https://andseleznev-lab.github.io/-TGBOT/

### Что реализовано:
- ✅ Модалка согласия при первом входе (v63)
- ✅ Логирование в Supabase PostgreSQL через Make.com
- ✅ localStorage кеширование для UX
- ✅ Обработка ошибок webhook с понятными сообщениями
- ✅ Логирование согласия при оплате бронирований (v64)
- ✅ Логирование согласия при оплате клуба (v65)
- ✅ Московское время для timestamps (UTC+3) (v67)
- ✅ Юридическая защита по 152-ФЗ (consent logging)

### Версии:
- **v63**: Модалка согласия на политику конфиденциальности
- **v64**: Текст договора оферты + логирование при оплате бронирований
- **v65**: Текст договора оферты + логирование при оплате клуба
- **v66**: UX улучшения (порядок услуг + Eruda скрыта)
- **v67**: FIX - Московское время для consent timestamps (UTC+3)
