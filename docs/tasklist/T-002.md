# Tasklist: Хранение слотов в Git (JSON)

**Тикет**: T-002
**PRD**: `docs/prd/T-002.prd.md`
**План**: `docs/plan/T-002.md`
**Дата создания**: 2026-02-02
**Статус**: TASKLIST_READY

---

## 📊 Прогресс

**Всего задач**: 24
**Выполнено**: 24 (Этап 1: 7 задач, Этап 2: 8 задач (сделано вручную), Этап 3: 6 задач, Этап 4: 3 задачи)
**Прогресс**: [24/24] (100%)
**Статус**: COMPLETED

---

## ✅ Список задач

### Этап 1: Подготовка (без breaking changes)

- [ ] 1. Добавить CONFIG.SLOTS_JSON_URL в config.js
  - **Acceptance**:
    - Добавлена константа `SLOTS_JSON_URL` с GitHub raw URL
    - Добавлена константа `SLOTS_JSON_FALLBACK` (старый Make.com API)
    - URL корректный и рабочий
  - **Files**: `config.js`
  - **Estimate**: 5 мин

- [ ] 2. Создать функцию fetchSlotsFromGit()
  - **Acceptance**:
    - Функция загружает slots.json из GitHub raw URL
    - Проверяет кеш в localStorage (TTL 10 минут)
    - При ошибке (404, timeout) делает fallback на Make.com API
    - Сохраняет результат в кеш
    - Возвращает `{ slots: [...], generated_at: "..." }`
    - Есть JSDoc комментарий
  - **Files**: `app.js`
  - **Estimate**: 30 мин

- [ ] 3. Создать функцию filterSlotsByService(slots, serviceId)
  - **Acceptance**:
    - Функция фильтрует массив слотов по service
    - Возвращает отфильтрованный массив
    - Есть JSDoc комментарий
  - **Files**: `app.js`
  - **Estimate**: 10 мин

- [ ] 4. Создать функцию getAvailableDatesFromSlots(slots)
  - **Acceptance**:
    - Функция извлекает уникальные даты из слотов (только status === 'free')
    - Возвращает массив дат в формате 'YYYY-MM-DD'
    - Даты отсортированы по возрастанию
    - Есть JSDoc комментарий
  - **Files**: `app.js`
  - **Estimate**: 15 мин

- [ ] 5. Создать функцию getAvailableSlotsForDate(slots, date)
  - **Acceptance**:
    - Функция фильтрует слоты: date === указанная && status === 'free'
    - Возвращает отсортированный массив слотов по времени
    - Есть JSDoc комментарий
  - **Files**: `app.js`
  - **Estimate**: 15 мин

- [ ] 6. Протестировать новые функции в консоли
  - **Acceptance**:
    - Вызвать `fetchSlotsFromGit()` в консоли → слоты загружаются
    - Вызвать `filterSlotsByService(slots, 'package')` → корректная фильтрация
    - Вызвать `getAvailableDatesFromSlots(slots)` → корректный массив дат
    - Вызвать `getAvailableSlotsForDate(slots, '2026-02-03')` → корректные слоты
    - Нет ошибок в консоли
  - **Files**: Тестирование
  - **Estimate**: 15 мин

- [ ] 7. Коммит этапа 1
  - **Acceptance**:
    - Все функции добавлены, протестированы
    - Старый API ещё работает (не сломали ничего)
    - Коммит сообщение: `[T-002] Этап 1: Подготовка функций для работы со slots.json`
  - **Files**: `config.js`, `app.js`
  - **Estimate**: 5 мин

---

### Этап 2: Интеграция Make.com с GitHub API

**Примечание**: Задачи этого этапа выполняются вручную в Make.com (не через код)

- [x] 8. [Make.com] Создать Personal Access Token (PAT) в GitHub
  - **Acceptance**:
    - PAT создан в GitHub Settings → Developer settings → Personal access tokens
    - Права: `repo` (Full control of private repositories)
    - Токен скопирован и сохранён в безопасном месте
  - **Files**: GitHub Settings
  - **Estimate**: 10 мин
  - **Note**: Делается в интерфейсе GitHub

- [x] 9. [Make.com] Добавить GitHub PAT в Make.com
  - **Acceptance**:
    - Создано новое подключение "GitHub" в Make.com
    - Токен вставлен в Make.com
    - Подключение протестировано (Make.com может читать репозиторий)
  - **Files**: Make.com Settings
  - **Estimate**: 10 мин
  - **Note**: Делается в Make.com
  - **Completed**: 2026-02-02 (вручную)

- [x] 10. [Make.com] Добавить модуль "Read slots.json" в сценарий book_slot
  - **Acceptance**:
    - Модуль "HTTP: Make a request" добавлен после записи в Google Sheets
    - GET запрос к GitHub API: `/repos/{owner}/{repo}/contents/slots.json`
    - Заголовок: `Authorization: token {PAT}`
    - Response сохраняется (SHA и content)
  - **Files**: Make.com сценарий "Booking System"
  - **Estimate**: 20 мин
  - **Note**: Делается в Make.com
  - **Completed**: 2026-02-02 (вручную)

- [x] 11. [Make.com] Добавить модуль "Update slot status" в сценарий book_slot
  - **Acceptance**:
    - Парсим content из предыдущего модуля (base64 decode → JSON)
    - Находим слот по `slot_id` (из входных данных)
    - Проверяем: если `status === 'book'` → выходим с ошибкой "slot_already_taken"
    - Меняем `status: 'free'` → `status: 'book'`
    - Кодируем обновлённый JSON в base64
  - **Files**: Make.com сценарий "Booking System"
  - **Estimate**: 30 мин
  - **Note**: Делается в Make.com
  - **Completed**: 2026-02-02 (вручную)

- [x] 12. [Make.com] Добавить модуль "Commit slots.json" в сценарий book_slot
  - **Acceptance**:
    - Модуль "HTTP: Make a request" добавлен
    - PUT запрос к GitHub API: `/repos/{owner}/{repo}/contents/slots.json`
    - Body: `{ "message": "Update slot {slot_id} status to book", "content": "{base64_content}", "sha": "{sha}" }`
    - Заголовок: `Authorization: token {PAT}`
    - Логируем успех: "slots.json updated successfully"
  - **Files**: Make.com сценарий "Booking System"
  - **Estimate**: 20 мин
  - **Note**: Делается в Make.com
  - **Completed**: 2026-02-02 (вручную)

- [x] 13. [Make.com] Протестировать обновление slots.json при бронировании
  - **Acceptance**:
    - Сделать тестовое бронирование через фронтенд
    - Проверить: слот в slots.json изменил статус на "book"
    - Проверить Make.com логи: "slots.json updated successfully"
    - Проверить Git: коммит появился в истории
  - **Files**: Тестирование Make.com
  - **Estimate**: 15 мин
  - **Completed**: 2026-02-02 (вручную)

- [x] 14. [Make.com] Добавить обновление slots.json в сценарий cancel_booking
  - **Acceptance**:
    - Аналогично задачам 10-12, но:
    - Находим слот по `slot_id` из бронирования
    - Меняем `status: 'book'` → `status: 'free'`
    - Коммитим: "Update slot {slot_id} status to free"
  - **Files**: Make.com сценарий "Booking System"
  - **Estimate**: 25 мин
  - **Note**: Делается в Make.com
  - **Completed**: 2026-02-02 (вручную)

- [x] 15. [Make.com] Протестировать обновление slots.json при отмене
  - **Acceptance**:
    - Отменить тестовое бронирование
    - Проверить: слот в slots.json изменил статус на "free"
    - Проверить Make.com логи: "slots.json updated successfully"
  - **Files**: Тестирование Make.com
  - **Estimate**: 10 мин
  - **Completed**: 2026-02-02 (вручную)

---

### Этап 3: Переключение фронтенда на slots.json

- [x] 16. Рефакторить loadAvailableDates() на использование fetchSlotsFromGit()
  - **Acceptance**:
    - Заменить вызов `BookingAPI.getAvailableDates()` на:
      1. `const slotsData = await fetchSlotsFromGit()`
      2. `const serviceSlots = filterSlotsByService(slotsData.slots, State.selectedService)`
      3. `State.availableDates = getAvailableDatesFromSlots(serviceSlots)`
    - Обработка ошибок: при ошибке fetchSlotsFromGit() делает fallback автоматически
    - Логируем: "[loadAvailableDates] Загружено дат: X"
    - Функциональность не изменилась (календарь отображается как раньше)
  - **Files**: `app.js`
  - **Estimate**: 20 мин
  - **Completed**: 2026-02-02

- [x] 17. Рефакторить loadAvailableSlots() на использование fetchSlotsFromGit()
  - **Acceptance**:
    - Заменить вызов `BookingAPI.getAvailableSlots()` на:
      1. `const slotsData = await fetchSlotsFromGit()`
      2. `const serviceSlots = filterSlotsByService(slotsData.slots, State.selectedService)`
      3. `State.availableSlots = getAvailableSlotsForDate(serviceSlots, date)`
    - Обработка ошибок: при ошибке fetchSlotsFromGit() делает fallback
    - Логируем: "[loadAvailableSlots] Загружено слотов для {date}: X"
    - Слоты отображаются корректно
  - **Files**: `app.js`
  - **Estimate**: 20 мин
  - **Completed**: 2026-02-02

- [x] 18. Добавить инвалидацию кеша после бронирования
  - **Acceptance**:
    - В функции `confirmBooking()` после успешного ответа:
    - Вызвать `CacheManager.clear('slots_json')` для инвалидации кеша
    - Логировать: "🗑️ Кеш slots.json инвалидирован после бронирования"
    - При следующей загрузке слоты обновятся из Git
  - **Files**: `app.js`
  - **Estimate**: 10 мин
  - **Completed**: 2026-02-02

- [x] 19. Добавить инвалидацию кеша после отмены бронирования
  - **Acceptance**:
    - В функции `cancelBooking()` после успешного ответа:
    - Вызвать `CacheManager.clear('slots_json')`
    - Логировать: "🗑️ Кеш slots.json инвалидирован после отмены"
  - **Files**: `app.js`
  - **Estimate**: 10 мин
  - **Completed**: 2026-02-02

- [x] 20. Коммит этапа 3
  - **Acceptance**:
    - Фронтенд переключен на slots.json из Git
    - Fallback на старый API работает
    - Коммит сообщение: `[T-002] Этап 3: Фронтенд переключен на slots.json из Git`
  - **Files**: `app.js`
  - **Estimate**: 5 мін
  - **Completed**: 2026-02-02 (commit fb88d11)

- [x] 20a. Удалить Make.com API для read-операций (оставить только booking)
  - **Acceptance**:
    - Удалены методы: `getServices()`, `getAvailableDates()`, `getAvailableSlots()`
    - Обновлён массив `readOnlyActions` - оставлен только `'get_user_bookings'`
    - Упрощён fallback в `fetchSlotsFromGit()` - убран Make.com, только кеш
    - Удалена константа `CONFIG.SLOTS_JSON_FALLBACK`
    - Оставлены только write методы: `bookSlot()`, `getUserBookings()`, `cancelBooking()`
    - Read операции полностью через slots.json из Git
  - **Files**: `app.js`, `config.js`
  - **Estimate**: 20 мін
  - **Completed**: 2026-02-07 (commit 6effb4c)

---

### Этап 4: Тестирование

- [ ] 21. Протестировать основной сценарий (Happy Path)
  - **Acceptance**:
    - Открыть приложение, выбрать услугу
    - Календарь загружается < 500ms
    - Выбрать дату → слоты отображаются мгновенно (из кеша)
    - Забронировать слот → успешно
    - Открыть slots.json в браузере → статус слота "book"
    - Отменить бронирование → слот вернулся в "free"
    - Нет ошибок в консоли
  - **Files**: Тестирование
  - **Estimate**: 20 мин

- [ ] 22. Протестировать edge cases
  - **Acceptance**:
    - **GitHub недоступен**: заблокировать GitHub в DevTools → fallback на Make.com API работает
    - **Слот уже занят**: два устройства бронируют один слот → второе получает ошибку "slot_already_taken"
    - **Кеш устарел**: очистить localStorage → слоты перезагружаются
    - **Быстрая смена услуг**: быстро переключать услуги → нет багов/лагов
    - Во всех случаях приложение работает корректно
  - **Files**: Тестирование
  - **Estimate**: 30 мин

- [ ] 23. Финальная проверка и документация
  - **Acceptance**:
    - Все задачи выполнены
    - Обновлён `CHANGELOG.md`: добавлена запись о переходе на slots.json
    - Все функции имеют JSDoc комментарии
    - Нет console.log в production коде (только console.error для ошибок)
    - Приложение работает стабильно
    - Готово к финальному коммиту
  - **Files**: `CHANGELOG.md`, все изменённые файлы
  - **Estimate**: 15 мин

---

## 📝 Примечания по задачам

### Задача 2 (fetchSlotsFromGit)
**Важно**: Эта функция использует уже существующий CacheManager из T-001. Кеш ключ: `'slots_json'`, TTL: 10 минут (600000 ms).

### Задачи 8-15 (Make.com)
Эти задачи выполняются вручную в интерфейсе Make.com. Не требуют написания кода, только настройку модулей Make.com и GitHub API интеграции.

### Fallback на старый API
Fallback должен быть прозрачным для пользователя. Никаких дополнительных popup или индикаторов. Логируем только в консоль для отладки.

---

## 🔄 История выполнения

| Дата | Задача | Комментарий |
|------|--------|-------------|
| 2026-02-02 | Создание | Tasklist готов, приступаем к работе |
| 2026-02-02 | Этап 1 (1-7) | Подготовка функций завершена (commit 6454e2d) |
| 2026-02-02 | Этап 2 (8-15) | Make.com интеграция выполнена вручную |
| 2026-02-02 | Этап 3 (16-20) | Фронтенд переключен на slots.json (commit fb88d11) |
| 2026-02-07 | Этап 3 (20a) | Удалён Make.com API для read-операций (commit 6effb4c) |

---

## ⚠️ Проблемы и блокеры

### Активные проблемы

*Пока нет*

### Решённые проблемы

*Пока нет*

---

## ✅ Критерии завершения тикета

Тикет считается завершённым когда:

- [ ] Все задачи в tasklist выполнены (`[x]`)
- [ ] Фронтенд загружает слоты из slots.json < 500ms
- [ ] Бронирование/отмена обновляют slots.json в Git
- [ ] Fallback на Make.com API работает при недоступности GitHub
- [ ] Тесты (manual) зелёные
- [ ] Документация обновлена (CHANGELOG.md)
- [ ] Нет известных critical багов
- [ ] Код закоммичен
- [ ] Статус изменён на `COMPLETED`

---

## 💡 Советы по работе с tasklist

1. **Работай последовательно** - этапы идут по порядку (1→2→3→4)
2. **Этап 1**: подготовка без breaking changes (можно откатить)
3. **Этап 2**: настройка Make.com (делается вручную в интерфейсе)
4. **Этап 3**: переключение фронтенда (критический момент, тестируй сразу)
5. **Этап 4**: полное тестирование перед финализацией
6. **Коммить после каждого этапа** - проще откатить если что-то пойдёт не так
7. **Не спеши** - этап 2 (Make.com) самый важный, проверяй каждый модуль
