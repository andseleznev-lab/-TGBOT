# PRD: Улучшение обработки сетевых ошибок

**Тикет**: T-001
**Автор**: Claude Code
**Дата создания**: 2026-01-31
**Статус**: PRD_READY

---

## 📋 Краткое описание

Улучшить обработку сетевых ошибок при загрузке данных из Make.com, обеспечив понятную диагностику проблем и возможность повторного запроса для пользователя.

---

## 🎯 Проблема / Возможность

### Контекст

Из логов Eruda DevTools (LogIos/Логи31.01v3.txt, line 200-204) выявлена проблема:
- При запросе `get_available_dates` происходит ошибка `TypeError: Load failed` после 2237ms
- Пользователю показывается общее сообщение "Не удалось загрузить доступные даты"
- Нет диагностики причины (сеть, таймаут, ошибка сервера)
- Нет возможности повторить запрос

Согласно conventions.md (line 294-304), обработка ошибок должна быть более детальной с проверкой типа ошибки и понятными сообщениями.

### Текущая ситуация (AS IS)

При сетевой ошибке:
1. Fetch выбрасывает `TypeError: Load failed`
2. Показывается общий popup "Не удалось загрузить доступные даты"
3. Пользователь не знает причину (нет сети, проблема сервера, таймаут)
4. Нет кнопки "Повторить"
5. Технические логи не различают типы ошибок

### Желаемая ситуация (TO BE)

При сетевой ошибке:
1. Система определяет тип ошибки (сеть, таймаут, сервер, отмена)
2. Показывается понятное сообщение:
   - "Проверьте интернет-соединение" (сеть)
   - "Сервер не отвечает. Попробуйте позже" (таймаут)
   - "Произошла ошибка. Попробуйте позже" (остальное)
3. Есть кнопка "Повторить попытку"
4. Технические логи содержат детали для отладки
5. HapticFeedback error при ошибках

---

## 👥 Целевая аудитория

**Основные пользователи**:
- Все пользователи Mini App, которые бронируют консультации

**Сценарии использования**:
- Пользователь пытается загрузить доступные даты при плохом интернете
- Пользователь пытается создать бронирование при проблемах с Make.com
- Пользователь просматривает свои бронирования при сетевых проблемах

---

## 🔍 Требования

### Функциональные требования

#### Must Have (обязательно)
- [x] Определение типа сетевой ошибки (Network, Timeout, Server, Abort)
- [x] Понятные сообщения об ошибках для каждого типа
- [x] Кнопка "Повторить попытку" в popup ошибок
- [x] HapticFeedback при ошибках
- [x] Детальное логирование ошибок для отладки

#### Should Have (желательно)
- [x] Автоматический retry с экспоненциальной задержкой (1 раз)
- [x] Индикатор "Повторная попытка..." во время retry
- [x] Таймаут для запросов (10 секунд)

#### Could Have (можно добавить позже)
- [ ] Offline detection (проверка navigator.onLine)
- [ ] Показ статуса сети в UI
- [ ] Накопление ошибок для analytics

#### Won't Have (не делаем в этом релизе)
- [ ] Автоматическая очередь запросов при появлении сети
- [ ] Кеширование запросов для offline режима

### Нефункциональные требования (NFR)

**Производительность**:
- Timeout для запросов: 10 секунд
- Retry задержка: 2 секунды (экспоненциально)
- Не более 1 автоматического retry

**Безопасность**:
- Не логируем чувствительные данные (user_id, personal info)
- Логируем только технические детали ошибок

**UX/UI**:
- Сообщения на русском языке, понятные пользователю
- HapticFeedback error при ошибках
- Кнопка "Повторить" в popup
- Индикатор загрузки во время retry

**Совместимость**:
- Работает на iOS 15+, Android 8+
- Корректно работает в Telegram WebApp

---

## 📱 Сценарии использования

### Основной сценарий (Happy Path)

**Действующие лица**: Пользователь, Система

**Предусловия**:
- Пользователь в приложении
- Пытается загрузить данные (даты, слоты, бронирования)

**Шаги**:
1. Пользователь переходит на экран Booking
2. Система начинает запрос `get_available_dates`
3. **Ошибка**: сеть недоступна (TypeError: Load failed)
4. Система определяет тип ошибки: Network Error
5. Система логирует детали: `[get_available_dates] Network error: Load failed`
6. Система показывает popup: "Проверьте интернет-соединение"
7. Система запускает HapticFeedback error
8. В popup есть кнопка "Повторить попытку"
9. Пользователь нажимает "Повторить"
10. Система повторяет запрос
11. Запрос успешен → данные загружены

**Результат**:
- Пользователь понимает причину ошибки
- Может повторить запрос без перезагрузки приложения
- Данные успешно загружены

### Альтернативные сценарии

#### Сценарий 2: Таймаут запроса

1. Пользователь переходит на экран Booking
2. Система начинает запрос с timeout 10 секунд
3. **Ошибка**: запрос превышает 10 секунд
4. Система отменяет запрос (AbortController)
5. Система показывает: "Сервер не отвечает. Попробуйте позже"
6. Пользователь может повторить или вернуться назад

#### Сценарий 3: Автоматический retry

1. Пользователь создаёт бронирование
2. Запрос fails (сеть)
3. Система автоматически делает retry через 2 секунды
4. Показывается индикатор "Повторная попытка..."
5. Retry успешен → бронирование создано
6. Показывается success сообщение

### Исключительные ситуации (Edge Cases)

**Ошибка 1**: Отмена запроса (AbortError)
- **Причина**: Пользователь быстро переключает табы
- **Обработка**: Молча игнорируем (не показываем ошибку)
- **Сообщение пользователю**: Нет (это нормальное поведение)

**Ошибка 2**: Сервер вернул HTTP 500
- **Причина**: Ошибка на стороне Make.com
- **Обработка**: Логируем, показываем popup
- **Сообщение пользователю**: "Сервер временно недоступен. Попробуйте позже"

**Ошибка 3**: Множественные неудачные retry
- **Причина**: Устойчивые проблемы с сетью/сервером
- **Обработка**: После 1 retry показываем финальную ошибку
- **Сообщение пользователю**: "Не удалось загрузить данные. Проверьте соединение и попробуйте позже"

---

## 🎨 UI/UX Дизайн

### Макеты

Используем стандартный Telegram WebApp popup с кнопками.

### Основные элементы интерфейса

**Popup ошибки**:
```javascript
tg.showPopup({
  title: 'Ошибка сети',
  message: 'Проверьте интернет-соединение',
  buttons: [
    { id: 'retry', type: 'default', text: 'Повторить' },
    { type: 'cancel' }
  ]
});
```

**Индикатор retry**:
- Текст: "Повторная попытка..."
- Loader/спиннер
- Overlay с полупрозрачным фоном

### Взаимодействия

- **Клик на "Повторить"**: запускается повторный запрос
- **Клик на "Отмена"**: закрывается popup, пользователь остаётся на экране
- **Haptic Feedback**: error при показе ошибки, success при успехе после retry

---

## 🔗 Зависимости

### Внешние системы
- **Make.com**: все существующие маршруты (get_available_dates, get_time_slots, create_booking, get_bookings, etc.)
- Изменения не требуются на стороне Make.com

### Внутренние зависимости
- **Файлы**: `js/app.js` (основная логика обработки ошибок)
- **Компоненты**: все функции, делающие fetch к Make.com
- **Другие тикеты**: Нет зависимостей

---

## 📊 Метрики успеха

### Как измерим успех фичи?

**Количественные метрики**:
- Снижение количества непонятных ошибок в логах на 80%
- Увеличение успешных retry на 50%
- Время обработки ошибки < 1 секунда

**Качественные метрики**:
- Пользователи понимают причину ошибки
- Пользователи могут самостоятельно решить проблему (проверить сеть)
- Нет жалоб на "непонятные ошибки"

---

## ⚠️ Риски и ограничения

### Технические риски

- **Риск 1**: Ложное определение типа ошибки
  - **Вероятность**: Низкая
  - **Влияние**: Среднее (неправильное сообщение пользователю)
  - **Митигация**: Тестирование всех типов ошибок, fallback на общее сообщение

- **Риск 2**: Автоматический retry создаёт дубли (для create_booking)
  - **Вероятность**: Средняя
  - **Влияние**: Высокое (дублирование бронирований)
  - **Митигация**: Не делать автоматический retry для мутирующих операций (create, update, delete)

### Ограничения

- Telegram WebApp popup имеет ограничения по количеству кнопок (max 3)
- Нет доступа к детальной информации о сети (только navigator.onLine)
- Не можем различить 3G/4G/5G/WiFi

---

## ❓ Открытые вопросы

- [x] Делать ли автоматический retry для всех типов запросов?
  - **Решение**: Только для GET запросов (чтение), не для POST (создание)
- [x] Сколько retry попыток делать?
  - **Решение**: 1 автоматический retry, затем manual retry через кнопку
- [x] Какой timeout для запросов?
  - **Решение**: 10 секунд

---

## 📝 Решения и уточнения

### 2026-01-31 - Решение по retry стратегии

Автоматический retry только для GET запросов (get_available_dates, get_time_slots, get_bookings).
Для POST запросов (create_booking, update_booking, cancel_booking) - только manual retry через кнопку.

Причина: избежать дублирования операций при сетевых проблемах.

---

## 🔄 История изменений

| Дата | Версия | Автор | Изменения |
|------|--------|-------|-----------|
| 2026-01-31 | 1.0 | Claude Code | Первая версия PRD_READY |

---

## ✅ Чек-лист готовности PRD

Перед переходом к этапу планирования убедись:

- [x] Понятна проблема и цель фичи
- [x] Описаны все must-have требования
- [x] Расписан happy path и основные edge cases
- [x] Определены метрики успеха
- [x] Выявлены риски и зависимости
- [x] Все блокирующие вопросы закрыты
- [x] Статус изменён на `PRD_READY`

**Только после этого можно переходить к созданию плана!**
