# CLAUDE.md 

## Главное правило

**ВСЕГДА** первым делом читай и следуй `conventions.md` перед любыми изменениями в коде.

## 📋 Перед началом работы над задачей

### 1. Найди активный тикет

Посмотри файл **`docs/.active_ticket`** - там указан ID текущей задачи.

Формат файла:

T-104

### 2. Изучи контекст тикета

Для тикета `T-XXX` прочитай (если существуют):

- **`docs/prd/T-XXX.prd.md`** - ЧТО делаем и ЗАЧЕМ (Product Requirements)
- **`docs/plan/T-XXX.md`** - КАК делаем (архитектура, технические решения)
- **`docs/tasklist/T-XXX.md`** - СПИСОК задач с чекбоксами

### 3. Сформулируй план изменений

**ПЕРЕД** любыми правками кода:

1. Опиши какие файлы будешь менять
2. Какие конкретно изменения внесёшь
3. Какие edge cases учтёшь
4. Как протестируешь изменения
5. **Дождись моего явного "ОК"**, **"GO"** или **"Давай"**

**Пример плана**:
```
План изменений для задачи T-104-3:

Файлы:
- js/app.js: добавлю функцию validateBookingData()
- css/styles.css: стили для сообщений об ошибках

Изменения:
1. Создам validateBookingData(data) с проверками:
   - service_id (обязательное, строка)
   - date (обязательное, валидная дата)
   - time (обязательное, формат HH:MM)
   - user_id (обязательное, число)

2. Добавлю визуальные индикаторы ошибок:
   - Красная обводка для невалидных полей
   - Текст ошибки под полем

3. Интегрирую в createBooking():
   - Валидация перед отправкой
   - Показ ошибок пользователю
   - Haptic feedback при ошибке

Edge cases:
- Пустые значения
- Неверный формат данных
- Прошедшие даты
- Несуществующий service_id

Тестирование:

- Попытка забронировать без выбора услуги
- Попытка забронировать без даты/времени
- Попытка забронировать прошедшую дату

Жду подтверждения!
```

### Работа с кодом

#### 1. Минимальные атомарные изменения

- Меняй **только** то, что нужно для текущей задачи
- НЕ рефактори "заодно" без явного запроса
- НЕ меняй то, что работает и не относится к задаче

#### 2. Комментарии и документация

- Обязательльно комментируй описание все необходиме действий и описание фукнций 
- Обязательные комментарии в документах 

**Язык комментариев**: русский (как в примерах выше)

#### 3. Соблюдай стиль проекта

Из `conventions.md`:
- **ES6+**: const/let, arrow functions, async/await
- **Именование**: camelCase для переменных/функций
- **Отступы**: 2 пробела
- **Точки с запятой**: обязательны
- **Кавычки**: одинарные `'text'`



### Работа с Make.com Backend

**Важно**: у тебя **НЕТ прямого доступа** к Make.com!

Что ты можешь:
- Предлагать изменения в структуре запросов

- Предложить свои варианты (коротко)

- После внесения изменений обнови docs/make-routes.md


## После внесения изменений

### 1. Покажи diff

Кратко опиши что изменилось в каждом файле:

Пример:

app.js: +25 -5
  - Интегрирована валидация в createBooking()
  - Добавлена обработка ошибок валидации

styles.css: +18 -0
  - Стили для .error-message
  - Стили для .input-error (красная обводка)

docs/tasklist/T-104.md: +1 -1
  - Отмечена задача #3 как выполненная [x]


### 2. Предложи текст коммита

Формат: `[TICKET-ID] Краткое описание`

[T-104] Добавлена валидация данных бронирования

- Реализована validateBookingData() с проверкой всех полей
- Обработка edge cases (пустые значения, неверный формат)
- Визуальные индикаторы ошибок
- HapticFeedback при ошибках валидации
- Обновлены тесты


### 3. Обнови tasklist

Поставь `[x]` напротив выполненной задачи в `docs/tasklist/T-XXX.md`:

```markdown
- [x] 3. Добавить валидацию формы бронирования
```

### 4. Остановись и жди

**НЕ переходи** к следующей задаче без моей команды:
- "Следующая задача"
- "Продолжай"
- "Делай дальше"

Это важно для контроля качества!

---

## Работа с ошибками

### Когда код не работает

Алгоритм действий:

1. **Проанализируй ошибку**:
   - Что именно сломалось?
   - Какая ошибка в консоли?
   - На каком этапе происходит?
   - Какие данные передаются?

2. **Найди минимальный фикс**:
   - Что нужно изменить чтобы исправить?
   - Не затронет ли это другой код?

3. **Объясни причину**:
   - Почему возникла ошибка?
   - Как фикс её решает?

**Пример**:
```
❌ Ошибка: "Cannot read property 'id' of undefined"

Анализ:
- Возникает в функции createBooking()
- Переменная selectedService undefined
- Причина: пользователь не выбрал услугу

Минимальный фикс:
- Добавить проверку перед созданием бронирования
- Показать ошибку "Выберите услугу"


### Когда не уверен в решении

**Будь честен!** Скажи об этом и предложи варианты:

Я вижу 2 способа решить эту задачу:

Вариант 1: Валидация на фронтенде
Плюсы: быстрая обратная связь, меньше нагрузки на Make
Минусы: можно обойти через DevTools

Вариант 2: Валидация в Make.com
Плюсы: 
Минусы: 

Вариант 3: Валидация в обоих местах (рекомендую)
Плюсы: 
Минусы: 

Какой вариант предпочитаешь?


---

##  Специфика проекта

### Performance критично!

**Почему**: Mini App на мобильных, медленный интернет, Make.com платный

**Что делать**:

1. **Избегай лишних API вызовов**
   ```javascript
   ПЛОХО - грузим при каждом открытии таба

   async function showServicesScreen() {
     const services = await fetchServices(); // API вызов!
     renderServices(services);
   }
   
   ХОРОШО - услуги хардкод в CONFIG
   function showServicesScreen() {
     renderServices(CONFIG.SERVICES); // Мгновенно!
   }
   ```

2. **Отменяй предыдущие запросы**
   ```javascript
   let currentRequest = null;
   
   async function fetchData() {
     // Отменяем старый запрос
     if (currentRequest) {
       currentRequest.abort();
     }
     
     currentRequest = new AbortController();
     // ... fetch с signal: currentRequest.signal
   }
   ```

3. **Используй debouncing**
   ```javascript
   // Для частых событий (клики по табам, навигация)
   const handleTabClick = debounce((tabId) => {
     switchTab(tabId);
   }, 500); // 500ms задержка
   ```

### Мобильный контекст

**Всё тестируем на телефоне!**

**Инструменты**:
- Eruda DevTools (подключён в index.html)
- Telegram WebApp Preview (в @BotFather)

**Особенности**:
- Touch events (не mouse events)
- Маленький экран (320px+)
- Медленный интернет (3G)
- Жесты назад/вперёд

### Дизайн система (Beige + Glassmorphism)

**Цвета** (из CSS Variables):
- Primary: `#D4A574` (accent-primary)
- Background: `linear-gradient(135deg, #FAF3E0 0%, #F5E6D3 100%)`
- Text: `#4A4238` (text-primary)
- Glass: `rgba(255, 252, 248, 0.4)` (glass-bg)

**При создании новых элементов**:
```css
.new-element {
  background: var(--glass-bg);
  backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid var(--glass-border);
  border-radius: var(--border-radius-lg); /* 28px */
  color: var(--text-primary);
}
```

**НЕ меняй цветовую схему** без явного указания!

---

## Запрещённые действия

 **НИКОГДА НЕ делай без разрешения**:

1. **НЕ коммить** изменения автоматически
2. **НЕ удалять** код без явного указания  
3. **НЕ менять** цветовую палитру и стили оставь как есть сейчас, пока явно не укажу что надо менять 
4. **НЕ добавлять** новые зависимости (библиотеки) предложить с обоснованием 
5. **НЕ рефакторить** код "заодно"
6. **НЕ переходить** к следующей задаче без команды

---

## Разрешённые и приветствуемые действия

1. **Указывать на проблемы**
   - "Заметил, что функция X может упасть при Y"
   - "Этот код может вызвать утечку памяти"
   - "Здесь нарушается принцип из conventions.md" 
   - "Здесь в инструкции claude.md есть правило мешающее разработке (указать какое)

2. **Предлагать улучшения**
   - "Предлагаю оптимизировать этот участок"
   - "Можно добавить кеширование для этого запроса"
   - "Лучше использовать debouncing здесь"

3. **Дополнять документацию**
   - Обновлять комментарии
   - Дописывать docs/make-routes.md
   - Улучшать README # создать при необходимости 

4. **Улучшать обработку ошибок**
   - Добавлять try/catch где нужно
   - Делать понятные сообщения для пользователя
   - Логировать технические детали

5. **Оптимизировать производительность**
   - Предлагать кеширование
   - Уменьшать количество API вызовов
   - Оптимизировать рендеринг

---

## Режимы работы

### Режим "Быстрый фикс" (Hotfix)

Для **срочных багов** в production:

1. Опиши проблему кратко
2. Предложи минимальный фикс
3. Примени после подтверждения  
4. **Создай тикет** для полноценного решения позже

**Пример**:
```
HOTFIX: Calendar не открывается

Проблема: TypeError при клике на "Забронировать"
Причина: selectedService undefined

Быстрый фикс:
if (!selectedService) return;

Полноценное решение (создать тикет):
- Добавить валидацию
- Показать ошибку пользователю
- Дизейблить кнопку если услуга не выбрана

Применить фикс? (после - создам тикет T-XXX)

### Режим "Новая фича" (Feature)


**Обязательно нужны**:

1. PRD (Product Requirements Document)
2. План (архитектура, технические решения)
3. Tasklist (список задач с чекбоксами)
4. Работа маленькими шагами

**Нельзя**:

- Сразу писать код без PRD
- Делать "всё и сразу" одним коммитом
- Пропускать планирование

### Режим "Рефакторинг"

Для **улучшения существующего кода**:

1. Опиши **зачем** нужен рефакторинг
2. Покажи план **до/после**
3. Гарантируй **сохранение функциональности**
4. Обнови **тесты** если нужно

**Пример**:
```
Рефакторинг функции createBooking()

Зачем:
- Сейчас 150 строк, сложно читать
- Дублирование кода валидации
- Нет разделения ответственности

План:
ДО:
- createBooking() - всё в одной функции

ПОСЛЕ:  
- validateBookingData() - валидация
- prepareBookingPayload() - подготовка данных
- sendBookingRequest() - API запрос
- handleBookingResponse() - обработка ответа
- createBooking() - оркестрация

Гарантия:
- Функциональность не изменится
- Все тесты останутся зелёными
- API контракт тот же

Одобряешь?
```

---

## Коммуникация

### Будь конкретным

**Плохо**: "Сделал изменения"
**Хорошо**: "Добавил валидацию email в форме бронирования"

**Плохо**: "Пофиксил баг"
**Хорошо**: "Исправил TypeError в createBooking() при пустом selectedService"

### Будь проактивным

Если видишь проблему - **сообщи**:

Заметил потенциальную проблему, пример:

В функции fetchAvailableDates() нет обработки случая когда Make.com 
вернёт пустой массив дат.

Это приведёт к тому что пользователь увидит пустой календарь без объяснения.

Предлагаю добавить:

Затем спросить: обавить в текущую задачу или создать отдельный тикет?
```

### Будь честным

Не знаешь ответа? **Скажи прямо**:

```
Честно говоря, я не уверен в лучшем подходе здесь.

Вижу 2 варианта:

Вариант 1: [описание]
Плюсы: ...
Минусы: ...

Вариант 2: [описание]  
Плюсы: ...
Минусы: ...

Какой выбрать? Или есть третий вариант который я упустил?


##  Финальное напоминание

Ты работаешь с проектом, где каждое изменение влияет на реальных пользователей.

### Принципы работы:

1. **Качество > Скорость**
   - Лучше медленно но правильно
   - Чем быстро и с багами

2. **Маленькие шаги > Большие изменения**
   - Атомарные коммиты
   - Итеративная разработка
   - Постоянная обратная связь

3. **Понятность > Краткость**
   - Лучше больше комментариев
   - Чем умный но непонятный код

4. **Безопасность > Функциональность**
   - Валидация обязательна
   - Обработка ошибок обязательна
   - Тестирование обязательно

5. **Пользователь > Технологии**
   - Понятные сообщения об ошибках
   - Плавная работа интерфейса
   - Быстрая обратная связь

---

## Что читать при работе

**Всегда**:
1. `conventions.md` - общие правила
2. `docs/prd/T-XXX.prd.md` - что делаем
3. `docs/tasklist/T-XXX.md` - список задач

**При необходимости**:
- `docs/plan/T-XXX.md` - архитектура решения
- `docs/make-routes.md` - документация Make.com маршрутов
- `README.md` - общая информация о проекте

